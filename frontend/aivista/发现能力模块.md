# AiVista 发现能力模块

> 对应迭代：前端迭代 7——推荐、关注与关键词搜索
> 当前状态：设计中
> 维护规则：本文记录发现能力的页面范围、交互、接口消费与验收；代码、测试、接口契约或交互规则变化时必须同步修改。

## 1. 目标与范围

在现有公开灵感首页和公开作品详情基础上，讨论并确定三类灵感列表的前端体验；后续讨论与实现只围绕这三类进行：

- 推荐列表。
- 关注列表。
- 关键词查询结果。

公开作品详情继续使用 `/inspirations/[imageId]` 及既有覆盖层规则。热门排序、评论、广告位和未经后端契约确认的静态入口不在范围内。

## 2. 已确认约束

- 发现结果只展示当前公开作品，图片项继续消费统一 `GenerationAssetImageResponse` 映射的前端模型。
- 站内点击任一公开作品时，详情通过拦截路由覆盖当前来源页面；直接访问或刷新详情 URL 时，以灵感首页为背景。
- 推荐、关注和关键词查询的分页均只能使用服务端返回的不透明游标；不得在前端推算页码、游标或推荐分数。
- 灵感页与发现能力不订阅 SSE；刷新或重新进入页面时读取新的服务端快照。
- 各类列表的匿名访问与登录要求尚待确认；无论访问者身份如何，均不得突破公开作品的可见性边界。
- 不在 URL、`localStorage`、埋点或错误监控中写入签名 URL、提示词或作品正文。

## 3. 关键决策记录

已确认灵感页只包含推荐列表、关注列表和关键词查询结果。以下事项待讨论确认后写入本表：入口信息架构、关键词查询触发方式与 URL 状态、推荐/关注列表切换模型、初始与继续加载规则、空态文案、移动端布局和可访问性细节。

| 决策事项 | 最终结论 | 原因与取舍 | 对实现的影响 |
| --- | --- | --- | --- |
| 灵感列表范围 | 推荐列表、关注列表、关键词查询结果 | 作为迭代 7 后续讨论的共同边界。 | 不呈现热门排序或其他未确认的发现入口。 |

## 4. 实施计划与进度

| 小步目标 | 状态 | 完成内容或当前阻塞 | 验证方式/结果 |
| --- | --- | --- | --- |
| 确认三类列表的页面信息架构与交互 | 进行中 | 推荐、关注与关键词查询的首版用户行为待讨论。 | 形成关键决策记录。 |
| 确认 API 映射、前端状态与错误处理 | 待开始 | 依赖后端接口设计。 | 文档设计准入检查。 |
| 前端实现与测试 | 待开始 | 不在设计完成前编码。 | 待确认。 |
| 联调与验收 | 待开始 | 依赖实现完成。 | 待确认。 |

## 5. 页面、路由与交互设计

待确认。设计完成后记录入口页面、推荐/关注/关键词查询切换、加载与空错误态、桌面/移动端差异、键盘操作和公开详情跳转规则。

## 6. API、前端模型与状态管理

待确认。设计完成后记录查询参数、响应 DTO 映射、游标缓存键、刷新与重试规则；后端业务事实以同名后端模块文档为准。

## 7. 组件与实现位置

待确认。设计中不预设不存在的路由、组件或状态文件。

## 8. 异常场景、可靠性与安全

待确认。需要覆盖无结果、非法或过宽查询、分页失败、接口限流、作品撤销、网络失败和认证状态变化。

## 9. 测试与验收

待确认。实现后记录自动化测试、构建、真实后端联调、桌面/移动端和可访问性验收；当前设计阶段不宣称测试结果。

## 10. 后续边界

热门排序、评论、复杂个性化、内容订阅、运营配置和商业化分发在具备独立需求与数据契约后另行设计。
