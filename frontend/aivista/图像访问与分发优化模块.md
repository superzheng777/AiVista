# AiVista 前端图像访问与分发优化模块

> 对应迭代：前端迭代 9——图像访问与分发优化
> 当前状态：设计中
> 维护规则：本文记录 CDN 图像 URL 的前端消费、缓存、刷新与失败处理；代码、测试、接口契约或交互规则变化时必须同步修改。

## 1. 目标与范围

在后端将图片读取 URL 切换为 CDN 时效签名 URL 后，保持资产、公开作品、详情、下载和复制的既有用户行为与私有信息边界。

## 2. 已确认约束

- 前端继续只消费图片 DTO 的 `url` 与到期时间，不拼接 CDN 路径、鉴权参数或 OSS 对象键。
- URL、签名、图片 DTO 和提示词仅保存在运行时内存，不写入页面 URL、持久化存储、埋点或错误监控。
- 公开图片详情只在 URL 已过期或实际读取失败时刷新单图 DTO；作者资产详情遵循相同的单图刷新规则。
- 撤销发布后，公开入口不再返回新 URL；作者仍可在个人资产入口查看同一图片。
- CDN 图片 URL 有效期初始为 10 分钟，CDN 边缘缓存为 7 天；浏览器缓存继续以 URL 有效期为上限。
- CDN 只允许实际生产前端域名及两个本地开发 Origin 的无凭证跨域读取，供前端下载和复制使用。

## 3. 关键决策记录

| 决策事项 | 最终结论 | 原因与取舍 | 对实现的影响 |
| --- | --- | --- | --- |
| URL 消费 | 前端保持域名无关，只消费后端返回 URL。 | CDN 切换不应扩散为前端存储或签名逻辑。 | 既有领域模型和 URL 刷新入口尽量保持不变。 |
| 缓存与刷新 | 继续复用已加载图片；仅过期或实际读取失败时刷新单图。 | 避免定时刷新及无意义请求。 | 不新增 CDN 专用轮询器。 |
| 撤销后行为 | 公开详情按当前后端结果处理；作者资产不受公开状态改变影响。 | 公开权限与所有权权限不同。 | 维持现有 404/不可读提示和资产详情行为。 |
| 下载与复制 | 统一通过 `fetch → Blob → Object URL` 读取 CDN 图片。 | 跨域 `<a download>` 的下载行为在浏览器间不一致；复制也必须读取 Blob。 | CDN 配置精确 Origin CORS；读取失败时按单图刷新一次后重试。 |

## 4. 实施计划与进度

| 小步目标 | 状态 | 完成内容或当前阻塞 | 验证方式/结果 |
| --- | --- | --- | --- |
| 前端边界确认 | 已完成 | 已确认 URL 不透明消费、内存边界与单图刷新规则。 | 本文第 2、3 节。 |
| API 契约对照 | 待开始 | 核对 CDN 切换后 DTO 字段与错误语义不变。 | 类型检查与接口联调。 |
| 页面与下载链路回归 | 待开始 | 验证资产、发现、详情、下载、复制和撤销后的显示。 | 浏览器人工验收。 |
| 缓存与异常回归 | 待开始 | 验证过期、403、404、CDN 故障和单图刷新一次的行为。 | 自动化测试与真实环境验收。 |

## 5. 页面、路由与交互设计

待实施。现有页面、路由和图片组件不因 CDN 域名变化新增页面或入口。

## 6. API、前端模型与状态管理

图片模型继续使用后端返回的 URL 与到期时间。禁止为 CDN 新增前端密钥、签名算法或持久化缓存；请求最新 URL 仍通过既有单图详情接口完成。下载与复制均以无凭证 `fetch` 读取当前 URL；前端不发送自定义鉴权 Header 或 Cookie。

## 7. 组件与实现位置

待实施。具体受影响的图片模型、资产详情、公开详情和下载/复制组件将在 API 契约确认后列出。

## 8. 异常场景、可靠性与安全

- URL 过期、CDN 403 或浏览器跨域读取失败时，最多刷新一次单图 DTO 后重试。
- 刷新仍失败时显示既有图片不可用提示，不展示 CDN、OSS、签名或对象键细节。
- CDN 鉴权 URL 不进入浏览器持久化缓存；页面继续使用 `Referrer-Policy: no-referrer`。

## 9. 测试与验收

| 类型 | 范围与结果 |
| --- | --- |
| 自动化验证 | 待实施：URL 复用、过期刷新、读取失败重试、撤销后的公开详情处理。 |
| 联调与人工验收 | 待实施：真实 CDN 的资产、公开详情、下载、复制、缓存与移动端表现。 |
| 未覆盖项 | CDN 尚未部署，当前不进行浏览器端实现或验收。 |

## 10. 后续边界

CDN 签名 Cookie、边缘 JWT 鉴权、离线图片、PWA 缓存、视频播放与图片编辑不进入本迭代。
