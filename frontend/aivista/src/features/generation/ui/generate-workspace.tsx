"use client";

import { type InfiniteData, useInfiniteQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { CheckCircle2, Clipboard, Download, Ellipsis, FolderClock, Heart, ImageOff, LoaderCircle, MessageSquare, PencilLine, Send, Sparkles, Trash2, X } from "lucide-react";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { useEffect, useRef, useState } from "react";
import { needsImageUrlRefresh, isActiveGenerationStatus, type GenerationAsset, type GenerationMessage, type GenerationSession, type GenerationTask } from "@/entities/generation/model/generation";
import { ImageDetailShell } from "@/entities/generation/ui/image-detail-shell";
import { assetQueryKeys, deleteGenerationAssets, getGenerationAsset, setGenerationImageFavorites } from "@/features/assets/api/asset-api";
import { downloadOriginalGenerationImage } from "@/features/assets/lib/original-image-download";
import { cancelGenerationTask, generationQueryKeys, listGenerationMessages, listGenerationSessions } from "@/features/generation/api/generation-api";
import { GenerationComposer } from "@/features/generation/ui/generation-composer";
import { useGenerationEventStream, type GenerationSessionIndicator } from "@/features/generation/model/generation-event-stream-provider";
import { mergeGenerationMessagePageData, type GenerationMessagePage } from "@/features/generation/model/generation-message-cache";
import { PublicationFormDialog } from "@/features/publication/ui/publication-form-dialog";
import { cn } from "@/lib/utils";
import { getApiErrorCode } from "@/shared/api/api-response";
import { AccentSquare, DotMatrix } from "@/shared/ui/editorial-ornaments/editorial-ornaments";

function taskStatusText(task: Pick<GenerationTask, "status" | "retryCount" | "maxRetryCount">): string {
  const retryProgress = `${task.retryCount}/${task.maxRetryCount}`;
  if (task.status === "QUEUED" && task.retryCount > 0) return `模型调用失败，正在重试（${retryProgress}）`;
  if (task.status === "QUEUED") return "已排队，正在等待生成";
  if (task.status === "RUNNING" && task.retryCount > 0) return `正在处理中（已重试 ${retryProgress}）`;
  if (task.status === "RUNNING") return "正在生成图片";
  if (task.status === "TRANSFERRING") return "图片已生成，正在保存";
  if (task.status === "SUCCEEDED") return "生成已完成";
  if (task.status === "PARTIALLY_SUCCEEDED") return "部分图片已生成";
  if (task.status === "FAILED") return "生成失败";
  if (task.status === "CANCELLED") return "任务已取消";
  return "尚未开始生成";
}

export function GenerateWorkspace() {
  const queryClient = useQueryClient();
  const router = useRouter();
  const searchParams = useSearchParams();
  const sessionId = searchParams.get("sessionId");
  const { sessionIndicators, syncVersion } = useGenerationEventStream();
  const sessionsQuery = useInfiniteQuery({ queryKey: generationQueryKeys.sessions(), queryFn: ({ pageParam }) => listGenerationSessions(pageParam), initialPageParam: undefined as string | undefined, getNextPageParam: (lastPage) => lastPage.nextCursor ?? undefined });
  const sessions = sessionsQuery.data?.pages.flatMap((page) => page.items);
  const activeSessionTitle = sessionId ? sessions?.find((session) => session.id === sessionId)?.title : undefined;
  useEffect(() => { if (syncVersion > 0) void queryClient.refetchQueries({ queryKey: generationQueryKeys.sessions(), type: "active" }); }, [queryClient, syncVersion]);
  function selectSession(nextSessionId: string): void { router.push(`/generate?sessionId=${encodeURIComponent(nextSessionId)}`); }

  return <section className="min-h-dvh overflow-x-hidden bg-[#f5f0e6] text-[#171612]"><div className="grid min-h-dvh lg:h-dvh lg:grid-cols-[320px_minmax(0,1fr)]">
    <aside className="hidden min-h-0 border-r border-[#d9cfbf] bg-[#fffdf7] lg:block"><div className="sticky top-0 flex h-dvh flex-col overflow-y-auto px-6 py-[30px]"><p className="pl-1 text-[11px] font-semibold leading-none tracking-[0.16em] text-[#716b61]"><span className="text-[#c95f3f]">02</span> / CREATE</p><div className="mt-[14px] flex items-center justify-between"><h1 className="text-2xl font-bold leading-tight">开启创作</h1><Sparkles className="size-5 text-[#716b61]" /></div><button type="button" onClick={() => router.push("/generate")} className="mt-6 inline-flex h-[52px] w-full items-center gap-3 rounded-[7px] bg-[#171612] pl-7 text-left text-base font-semibold text-[#fffdf7] transition-colors hover:bg-[#302e28] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c95f3f] focus-visible:ring-offset-2"><PencilLine className="size-[18px]" />新对话</button><div className="mt-5"><SessionList sessions={sessions} indicators={sessionIndicators} isLoading={sessionsQuery.isLoading} isError={sessionsQuery.isError} hasNextPage={sessionsQuery.hasNextPage} isFetchingNextPage={sessionsQuery.isFetchingNextPage} activeSessionId={sessionId} onSelect={selectSession} onLoadMore={() => void sessionsQuery.fetchNextPage()} onRetry={() => void sessionsQuery.refetch()} /></div></div></aside>
    {sessionId ? <ConversationPanel sessionId={sessionId} sessionTitle={activeSessionTitle} /> : <NewConversationPanel />}
  </div></section>;
}

function NewConversationPanel() { return <main className="relative flex min-h-[calc(100dvh-4rem)] min-w-0 flex-col overflow-hidden bg-[#f5f0e6] px-5 py-16 pb-24 sm:px-8 lg:min-h-dvh lg:px-12 lg:py-0"><WorkspaceDecorations /><div className="relative z-10 mx-auto flex w-full max-w-[1040px] flex-1 flex-col justify-center lg:-translate-y-5"><div className="text-center"><p className="text-[11px] font-semibold tracking-[0.16em] text-[#716b61]"><span className="text-[#c95f3f]">01</span> / START CREATING</p><h1 className="mt-6 text-[28px] font-bold leading-[1.25] tracking-tight sm:text-[32px] lg:text-[38px]">你好，想创作什么？</h1><p className="mt-[14px] text-[15px] leading-6 text-[#716b61]">写下第一个想法，提交后会自动建立创作会话。</p></div><div className="mt-10"><GenerationComposer /></div></div><ArchiveLine label="TURN AN IDEA INTO AN IMAGE" /></main>; }

function ConversationPanel({ sessionId, sessionTitle }: { sessionId: string; sessionTitle?: string }) {
  const queryClient = useQueryClient(); const { acknowledgeSession, sessionIndicators, syncVersion } = useGenerationEventStream();
  const [isComposerCollapsed, setIsComposerCollapsed] = useState(false);
  const [detailAsset, setDetailAsset] = useState<GenerationAsset | null>(null);
  const [publishAsset, setPublishAsset] = useState<GenerationAsset | null>(null);
  const [actionNotice, setActionNotice] = useState<string | null>(null);
  const messagesQuery = useInfiniteQuery<GenerationMessagePage, Error, InfiniteData<GenerationMessagePage>, ReturnType<typeof generationQueryKeys.messages>, string | undefined>({ queryKey: generationQueryKeys.messages(sessionId), queryFn: ({ pageParam }) => listGenerationMessages(sessionId, pageParam), initialPageParam: undefined as string | undefined, getNextPageParam: (lastPage) => lastPage.nextBefore ?? undefined, structuralSharing: mergeGenerationMessagePageData });
  const messages = messagesQuery.data ? [...messagesQuery.data.pages].reverse().flatMap((page) => page.items) : undefined;
  useEffect(() => { if (syncVersion > 0) void queryClient.refetchQueries({ queryKey: generationQueryKeys.messages(sessionId), type: "active" }); }, [queryClient, sessionId, syncVersion]);
  const cancelTask = useMutation({ mutationFn: cancelGenerationTask, onSettled: () => { void Promise.all([queryClient.invalidateQueries({ queryKey: generationQueryKeys.sessions() }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.messages(sessionId) })]); } });
  const favoriteMutation = useMutation({
    mutationFn: ({ imageId, favorite }: { imageId: string; favorite: boolean }) => setGenerationImageFavorites([imageId], favorite),
    onSuccess: () => void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.messages(sessionId) })]),
    onError: () => setActionNotice("收藏状态更新失败，请重试。"),
  });
  const deleteMutation = useMutation({
    mutationFn: (imageId: string) => deleteGenerationAssets([imageId]),
    onSuccess: (_result, imageId) => { setDetailAsset((asset) => asset?.id === imageId ? null : asset); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.messages(sessionId) })]); },
    onError: () => setActionNotice("删除失败，请重试。"),
  });
  useEffect(() => { if (sessionIndicators[sessionId] === "COMPLETED" || sessionIndicators[sessionId] === "ATTENTION") acknowledgeSession(sessionId); }, [acknowledgeSession, sessionId, sessionIndicators]);
  const activeMessageTask = messages?.find((message) => isActiveGenerationStatus(message.generation.status))?.generation;
  async function refreshAsset(imageId: string): Promise<GenerationAsset> {
    const refreshed = await getGenerationAsset(imageId);
    setDetailAsset((asset) => asset?.id === imageId ? refreshed : asset);
    await queryClient.refetchQueries({ queryKey: generationQueryKeys.messages(sessionId), type: "active" });
    return refreshed;
  }
  async function openAsset(asset: GenerationAsset): Promise<void> {
    try { setDetailAsset(needsImageUrlRefresh(asset.imageUrls.display) ? await refreshAsset(asset.id) : asset); }
    catch { setActionNotice("图片访问地址刷新失败，请稍后重试。 "); }
  }
  function requestPublish(asset: GenerationAsset): void {
    if (asset.publicationReviewStatus === "PENDING") return setActionNotice("该图片正在审核中。");
    if (asset.publicationReviewStatus === "APPROVED") return setActionNotice("该图片已发布。");
    setPublishAsset(asset);
  }
  if (detailAsset) return <><ImageDetailShell image={detailAsset} refreshImage={refreshAsset} allowCopy={detailAsset.publicationReviewStatus === "NONE"} onClose={() => setDetailAsset(null)} actions={<ConversationAssetActions asset={detailAsset} isFavoriteUpdating={favoriteMutation.isPending} isDeleting={deleteMutation.isPending} onFavorite={() => favoriteMutation.mutate({ imageId: detailAsset.id, favorite: !detailAsset.favorited })} onPublish={() => requestPublish(detailAsset)} onDelete={() => { if (window.confirm("确定删除这张图片？此操作无法撤销。")) deleteMutation.mutate(detailAsset.id); }} />} />{publishAsset ? <PublicationFormDialog asset={publishAsset} onClose={() => setPublishAsset(null)} onSuccess={() => { setPublishAsset(null); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.messages(sessionId) })]); }} /> : null}</>;
  return <main className="relative flex min-h-[calc(100dvh-4rem)] min-w-0 flex-col bg-[#f5f0e6] lg:h-dvh lg:min-h-0"><header className="flex min-h-[74px] items-center justify-between border-b border-[#d9cfbf] bg-[#fffdf7]/80 px-5 sm:px-8"><h1 className="min-w-0 truncate text-base font-bold sm:text-lg">{sessionTitle ?? "创作会话"}</h1><div className="ml-4 flex shrink-0 items-center gap-3"><Link href="/assets" className="inline-flex h-9 items-center gap-1.5 rounded-[6px] border border-[#d9cfbf] bg-[#fffdf7] px-3 text-sm font-medium transition hover:bg-[#f7e3d4] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c95f3f]"><FolderClock className="size-4" />资产库</Link></div></header><section aria-label="当前会话历史" onScroll={(event) => setIsComposerCollapsed(event.currentTarget.scrollHeight - event.currentTarget.scrollTop - event.currentTarget.clientHeight > 160)} className="min-h-0 flex-1 overflow-y-auto px-5 py-7 pb-[224px] sm:px-8 lg:px-12"><div className="mx-auto max-w-[1040px]">{actionNotice ? <p role="status" className="mb-4 rounded-[7px] border border-[#debda9] bg-[#fff3ea] px-4 py-3 text-sm text-[#ae4d33]">{actionNotice}</p> : null}{messagesQuery.isLoading ? <HistorySkeleton /> : null}{messagesQuery.isError ? <div role="alert" className="border border-[#c95f3f]/30 bg-[#fff3ea] px-4 py-3 text-sm text-[#ae4d33]"><p>历史对话加载失败，请重试。</p><button type="button" onClick={() => void (messagesQuery.hasNextPage ? messagesQuery.fetchNextPage() : messagesQuery.refetch())} className="mt-1 font-medium underline underline-offset-4">重试</button></div> : null}{messagesQuery.hasNextPage ? <div className="mb-5 flex justify-center"><button type="button" onClick={() => void messagesQuery.fetchNextPage()} disabled={messagesQuery.isFetchingNextPage} className="inline-flex min-h-10 items-center gap-2 rounded-[6px] border border-[#bfb3a2] bg-[#fffdf7] px-3 text-sm text-[#716b61] disabled:opacity-60">{messagesQuery.isFetchingNextPage ? <LoaderCircle className="size-4 animate-spin" /> : null}加载更早的对话</button></div> : null}{messages?.map((message) => <ConversationMessage key={message.id} message={message} onOpenAsset={openAsset} onRefreshAsset={refreshAsset} onFavorite={(asset) => favoriteMutation.mutate({ imageId: asset.id, favorite: !asset.favorited })} onPublish={requestPublish} onDelete={(asset) => { if (window.confirm("确定删除这张图片？此操作无法撤销。")) deleteMutation.mutate(asset.id); }} />)}{!messagesQuery.isLoading && !messages?.length ? <div className="flex min-h-56 items-center justify-center"><p className="text-sm text-[#716b61]">这个会话还没有可展示的历史内容。</p></div> : null}{activeMessageTask ? <div className="flex justify-start"><div className="w-full max-w-4xl"><TaskNotice task={activeMessageTask} isCancelling={cancelTask.isPending} cancellationError={cancelTask.error} onCancel={() => cancelTask.mutate(activeMessageTask.id)} /></div></div> : null}</div></section><div className="pointer-events-none absolute inset-x-0 bottom-0 z-10 px-5 pb-5 sm:px-8 lg:px-12"><div className="pointer-events-auto mx-auto max-w-[1040px]"><GenerationComposer sessionId={sessionId} hasActiveTask={Boolean(activeMessageTask)} compact={isComposerCollapsed} onExpand={() => setIsComposerCollapsed(false)} /></div></div>{publishAsset ? <PublicationFormDialog asset={publishAsset} onClose={() => setPublishAsset(null)} onSuccess={() => { setPublishAsset(null); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.messages(sessionId) })]); }} /> : null}</main>;
}

function SessionList({ sessions, indicators, isLoading, isError, hasNextPage, isFetchingNextPage, activeSessionId, onSelect, onLoadMore, onRetry }: { sessions: GenerationSession[] | undefined; indicators: Record<string, GenerationSessionIndicator>; isLoading: boolean; isError: boolean; hasNextPage: boolean; isFetchingNextPage: boolean; activeSessionId: string | null; onSelect: (sessionId: string) => void; onLoadMore: () => void; onRetry: () => void }) { return <div className="space-y-2">{isLoading ? <SessionSkeleton /> : null}{isError ? <div role="alert" className="px-2 py-3 text-sm text-[#ae4d33]"><p>会话加载失败，请重试。</p><button type="button" onClick={onRetry} className="mt-1 font-medium underline">重试</button></div> : null}{sessions?.map((session) => <SessionListItem key={session.id} session={session} indicator={activeSessionId === session.id ? undefined : indicators[session.id]} active={activeSessionId === session.id} onSelect={onSelect} />)}{hasNextPage ? <button type="button" onClick={onLoadMore} disabled={isFetchingNextPage} className="inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-[6px] px-3 text-sm text-[#716b61] disabled:opacity-60">{isFetchingNextPage ? <LoaderCircle className="size-4 animate-spin" /> : null}加载更多会话</button> : null}{!isLoading && !sessions?.length ? <p className="px-2 py-2 text-xs leading-5 text-[#716b61]">尚无历史会话。</p> : null}</div>; }

function SessionListItem({ session, indicator, active, onSelect }: { session: GenerationSession; indicator?: GenerationSessionIndicator; active: boolean; onSelect: (sessionId: string) => void }) { const latestTaskIsActive = session.latestTask ? isActiveGenerationStatus(session.latestTask.status) : false; const statusIndicator = latestTaskIsActive ? <LoaderCircle aria-label="正在生成" className="size-3.5 shrink-0 animate-spin text-[#c95f3f]" /> : indicator === "ATTENTION" ? <span aria-label="生成失败或已取消" className="size-2 shrink-0 bg-[#ae4d33]" /> : indicator === "COMPLETED" ? <CheckCircle2 aria-label="有新的生成结果" className="size-3.5 shrink-0 text-[#c95f3f]" /> : null; return <button type="button" onClick={() => onSelect(session.id)} className={cn("flex h-[52px] w-full items-center gap-[10px] rounded-[7px] px-[14px] text-left text-sm transition", active ? "border border-[#debda9] bg-[#f7e3d4] font-medium text-[#171612]" : "text-[#716b61] hover:bg-[#faf5eb] hover:text-[#171612]")}><MessageSquare className="size-[18px] shrink-0" /><span className="min-w-0 flex-1 truncate">{session.title}</span>{statusIndicator}{active ? <span aria-label="当前会话" className="size-[9px] shrink-0 bg-[#c95f3f]" /> : null}</button>; }

function ConversationMessage({ message, onOpenAsset, onRefreshAsset, onFavorite, onPublish, onDelete }: { message: GenerationMessage; onOpenAsset: (asset: GenerationAsset) => Promise<void>; onRefreshAsset: (imageId: string) => Promise<GenerationAsset>; onFavorite: (asset: GenerationAsset) => void; onPublish: (asset: GenerationAsset) => void; onDelete: (asset: GenerationAsset) => void }) { return <article className="py-7 first:pt-0"><div className="flex justify-start" aria-label="用户消息"><div className="w-fit max-w-full rounded-[7px] border border-[#debda9] bg-[#f7e3d4] px-4 py-3 text-[#171612]"><p className="whitespace-pre-wrap break-words text-sm leading-7">{message.prompt}</p>{message.negativePrompt ? <p className="mt-2 border-t border-[#debda9] pt-2 text-xs leading-5 text-[#716b61]">负面提示词：{message.negativePrompt}</p> : null}</div></div><div className="mt-5 flex justify-start" aria-label="AI 回复"><div className="w-full max-w-[700px] rounded-[10px] border border-[#d9cfbf] bg-[#fffdf7] p-4 shadow-[0_2px_4px_rgb(43_35_25_/_3%)] sm:p-[18px]"><div className="flex items-center justify-between gap-3"><p className="inline-flex items-center gap-2 text-sm font-semibold text-[#ae4d33]"><span className="size-[11px] bg-[#c95f3f]" />AiVista</p><span className="rounded-[5px] bg-[#fff3ea] px-2 py-1 text-xs font-medium text-[#c95f3f]">{taskStatusText(message.generation)}</span></div><TaskOutcome task={message.generation} />{message.generation.failureMessage ? <p role="alert" className="mt-2 text-xs leading-5 text-[#ae4d33]">{message.generation.failureMessage}</p> : null}{message.generation.images.length ? <div className={cn("mt-4 grid gap-1.5", imageGridClassName(message.generation.images.length))}>{message.generation.images.map((image) => <GenerationImageCard key={image.id} image={image} onOpen={() => void onOpenAsset(image)} onRefresh={() => onRefreshAsset(image.id)} onFavorite={() => onFavorite(image)} onPublish={() => onPublish(image)} onDelete={() => onDelete(image)} />)}</div> : null}</div></div><ConversationArchiveLine /></article>; }
function imageGridClassName(imageCount: number): string { if (imageCount === 1) return "max-w-[420px] grid-cols-1"; if (imageCount === 2 || imageCount === 4) return "grid-cols-2"; return "grid-cols-3"; }
function GenerationImageCard({ image, onOpen, onRefresh, onFavorite, onPublish, onDelete }: { image: GenerationAsset; onOpen: () => void; onRefresh: () => Promise<GenerationAsset>; onFavorite: () => void; onPublish: () => void; onDelete: () => void }) {
  const [menuOpen, setMenuOpen] = useState(false);
  const [imageUnavailable, setImageUnavailable] = useState(false);
  const [actionError, setActionError] = useState<string | null>(null);
  const retryUsedRef = useRef(false);
  async function retryImage(): Promise<void> { if (retryUsedRef.current) return setImageUnavailable(true); retryUsedRef.current = true; try { await onRefresh(); } catch { setImageUnavailable(true); } }
  async function download(): Promise<void> { try { await downloadOriginalGenerationImage(image); } catch { setActionError("下载失败，请稍后重试。"); } }
  async function copy(): Promise<void> { try { let current = image; if (needsImageUrlRefresh(current.imageUrls.display)) current = await onRefresh(); const fetchDisplay = async (asset: GenerationAsset) => { const url = asset.imageUrls.display?.url; if (!url) throw new Error("missing display"); const response = await fetch(url, { mode: "cors", referrerPolicy: "no-referrer" }); if (!response.ok) throw new Error("image fetch failed"); return response.blob(); }; let blob: Blob; try { blob = await fetchDisplay(current); } catch { blob = await fetchDisplay(await onRefresh()); } if (!navigator.clipboard?.write || typeof ClipboardItem === "undefined") throw new Error("clipboard unavailable"); await navigator.clipboard.write([new ClipboardItem({ [blob.type || "image/webp"]: blob })]); } catch { setActionError("复制失败，请使用下载。"); } }
  if (!image.imageUrls.thumbnail) return <div role="status" className="flex aspect-square flex-col items-center justify-center gap-2 rounded-[6px] border border-dashed border-[#bfb3a2] bg-[#faf5eb] px-4 text-center text-xs text-[#716b61]"><ImageOff className="size-5" />图片已从资产库删除</div>;
  return <div className="group relative overflow-visible rounded-[6px] border border-[#d9cfbf] bg-[#fffdf7]">
    <button type="button" onClick={onOpen} className="block w-full overflow-hidden rounded-[5px] text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c95f3f]">
      {imageUnavailable ? <span className="flex aspect-square items-center justify-center bg-[#faf5eb] text-xs text-[#716b61]">图片已从资产库删除</span> : <>
        {/* Private short-lived signed URLs must not be sent through an image optimizer. */}
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img src={image.imageUrls.thumbnail.url} alt="本次生成的图片" loading="lazy" decoding="async" referrerPolicy="no-referrer" onError={() => void retryImage()} className="aspect-square w-full object-cover transition duration-200 group-hover:scale-[1.015]" />
      </>}
    </button>
    <div className="absolute right-2 top-2 z-10 flex translate-y-1 items-center gap-1 rounded-[6px] border border-white/30 bg-[#171612]/90 p-1 text-[#fffdf7] opacity-0 shadow-lg transition duration-150 group-hover:translate-y-0 group-hover:opacity-100 focus-within:translate-y-0 focus-within:opacity-100"><button type="button" onClick={() => void download()} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-label="下载原图"><Download className="size-3.5" /></button><button type="button" onClick={() => void copy()} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-label="复制展示图"><Clipboard className="size-3.5" /></button><button type="button" onClick={() => setMenuOpen((open) => !open)} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-expanded={menuOpen} aria-label="更多图片操作"><Ellipsis className="size-4" /></button>{menuOpen ? <div className="absolute right-0 top-[calc(100%+6px)] w-28 overflow-hidden rounded-[6px] border border-[#d9cfbf] bg-[#fffdf7] py-1 text-[#171612] shadow-xl"><button type="button" onClick={() => { setMenuOpen(false); onFavorite(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs hover:bg-[#faf5eb]"><Heart className={cn("size-3.5", image.favorited && "fill-[#c95f3f] text-[#c95f3f]")} />{image.favorited ? "取消收藏" : "收藏"}</button><button type="button" onClick={() => { setMenuOpen(false); onPublish(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs hover:bg-[#faf5eb]"><Send className="size-3.5" />发布</button><button type="button" onClick={() => { setMenuOpen(false); onDelete(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs text-[#ae4d33] hover:bg-[#fff3ea]"><Trash2 className="size-3.5" />删除</button></div> : null}</div>{actionError ? <p role="status" className="absolute inset-x-1 bottom-1 rounded-[4px] bg-[#171612]/85 px-2 py-1 text-center text-[10px] text-white">{actionError}</p> : null}
  </div>;
}
function ConversationAssetActions({ asset, isFavoriteUpdating, isDeleting, onFavorite, onPublish, onDelete }: { asset: GenerationAsset; isFavoriteUpdating: boolean; isDeleting: boolean; onFavorite: () => void; onPublish: () => void; onDelete: () => void }) { return <section><p className="text-xs font-medium tracking-wide text-muted-foreground">作品操作</p><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" onClick={onFavorite} disabled={isFavoriteUpdating} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-border text-sm font-medium hover:bg-muted disabled:opacity-50"><Heart className={cn("size-4", asset.favorited && "fill-current text-[#c95f3f]")} />{asset.favorited ? "已收藏" : "收藏"}</button><button type="button" onClick={onPublish} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-border text-sm font-medium hover:bg-muted"><Send className="size-4" />发布</button></div><button type="button" onClick={onDelete} disabled={isDeleting} className="mt-2 inline-flex h-10 w-full items-center justify-center gap-2 rounded-lg bg-destructive/10 text-sm font-medium text-destructive hover:bg-destructive/20 disabled:opacity-50"><Trash2 className="size-4" />删除图片</button></section>; }
function TaskOutcome({ task }: { task: GenerationTask }) { const isTerminal = task.status === "SUCCEEDED" || task.status === "PARTIALLY_SUCCEEDED" || task.status === "FAILED" || task.status === "CANCELLED"; return isTerminal ? <p className="mt-2 text-xs leading-5 text-[#716b61]">结果：成功 {task.completedImageCount} 张 · 失败 {task.failedImageCount} 张 · 已取消 {task.cancelledImageCount} 张</p> : null; }
function TaskNotice({ task, isCancelling, cancellationError, onCancel }: { task: GenerationTask; isCancelling: boolean; cancellationError: unknown; onCancel: () => void }) { const [isConfirmingCancellation, setIsConfirmingCancellation] = useState(false); const isActive = isActiveGenerationStatus(task.status); const cancellationMessage = getApiErrorCode(cancellationError) === 40907 ? "任务已结束，状态已刷新。" : cancellationError ? "取消请求未完成，请重试。" : null; return <div className="mt-6 rounded-[7px] border border-[#debda9] bg-[#fff3ea] px-4 py-3 text-sm text-[#171612]"><div className="flex flex-wrap items-center justify-between gap-3"><span className="inline-flex items-center gap-2"><CheckCircle2 className="size-4 text-[#c95f3f]" />{isCancelling ? "正在取消任务，等待状态确认…" : taskStatusText(task)}</span>{isActive && !isConfirmingCancellation ? <button type="button" onClick={() => setIsConfirmingCancellation(true)} disabled={isCancelling} className="inline-flex min-h-10 items-center rounded-[6px] border border-[#bfb3a2] px-3 text-sm font-medium">取消生成</button> : null}</div><TaskOutcome task={task} />{isActive && isConfirmingCancellation ? <div className="mt-3 flex flex-wrap items-center gap-2 border-t border-[#debda9] pt-3"><p className="mr-auto text-xs text-[#716b61]">取消后本次任务将停止处理。</p><button type="button" onClick={() => setIsConfirmingCancellation(false)} disabled={isCancelling} className="min-h-10 rounded-[6px] px-3 text-sm font-medium">继续生成</button><button type="button" onClick={onCancel} disabled={isCancelling} className="inline-flex min-h-10 items-center gap-2 rounded-[6px] bg-[#171612] px-3 text-sm font-medium text-[#fffdf7]">{isCancelling ? <LoaderCircle className="size-4 animate-spin" /> : <X className="size-4" />}确认取消</button></div> : null}{cancellationMessage ? <p role="alert" className="mt-3 text-xs text-[#ae4d33]">{cancellationMessage}</p> : null}</div>; }
function WorkspaceDecorations() { return <div aria-hidden="true" className="pointer-events-none absolute inset-0 select-none"><span className="absolute right-[7%] top-[94px] hidden h-[390px] w-[290px] bg-[#f7e3d4] lg:block" /><div className="absolute left-[13%] top-[150px] hidden h-[57px] w-[109px] lg:block"><DotMatrix columns={5} rows={5} dotSize={3} gap={7} opacity={0.55} className="absolute left-0 top-0" /><AccentSquare size={17} className="absolute bottom-0 right-0" /></div></div>; }
function ArchiveLine({ label }: { label: string }) { return <div aria-hidden="true" className="relative z-10 mt-auto hidden items-center gap-[14px] pb-[58px] pt-8 lg:flex"><span className="size-2 shrink-0 bg-[#716b61]" /><span className="h-px flex-1 bg-[#bfb3a2]" /><span className="shrink-0 whitespace-nowrap text-[10px] font-medium tracking-[0.18em] text-[#716b61]">{label}</span><span className="size-2 shrink-0 bg-[#c95f3f]" /></div>; }
function ConversationArchiveLine() { return <div aria-hidden="true" className="my-6 flex items-center gap-3"><span className="h-px flex-1 bg-[#bfb3a2]" /><span className="whitespace-nowrap text-[10px] font-medium tracking-[0.18em] text-[#716b61]">CREATED FROM YOUR IDEA</span><span className="size-2 bg-[#c95f3f]" /><span className="h-px flex-1 bg-[#bfb3a2]" /></div>; }
function HistorySkeleton() { return <div className="space-y-6 animate-pulse"><div className="h-5 w-4/5 bg-[#e7ddce]" /><div className="h-64 max-w-xl rounded-[10px] bg-[#e7ddce]" /></div>; }
function SessionSkeleton() { return <div className="h-[52px] animate-pulse rounded-[7px] bg-[#f1e8dc]" />; }
