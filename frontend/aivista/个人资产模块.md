# AiVista 个人资产模块

> 对应迭代：前端迭代 3——个人资产
> 当前状态：实施中
> 维护规则：本文记录个人生成资产的页面交互、接口消费与验收；代码、测试、接口契约或交互规则变化时必须同步修改。

## 1. 目标与范围

接入已完成的个人资产后端能力，使已登录用户能浏览自己的成功生成图片、查看图片与最终提示词，并按单张或手动多选删除。

本迭代不包含收藏、发布、公开展示、点赞、关注和按全部历史一键删除。

后端业务事实与接口契约以 [个人生成历史与资产模块.md](../../backend/aivista/个人生成历史与资产模块.md) 为准。

## 2. 已确认约束

- 资产最小单位是单张成功图片；资产列表只显示当前用户且未删除的图片。
- 列表按成功保存时间倒序，前端使用 `Asia/Shanghai` 按日期分组；跨页结果追加到既有日期组。
- 使用不透明游标分页，默认 `limit=36`、最大 `60`，不显示或推测总数。
- 列表响应同时提供详情展示所需字段；点击图片不新增单图详情请求。
- 私有签名 URL 仅在内存中使用；URL 过期时重新加载当前资产列表。
- 删除使用统一批量接口；只提交用户手动勾选的 `imageIds`，重复删除按成功处理。
- 删除成功即从资产页移除；OSS 后台清理不阻塞前端，也不能恢复可见性。

## 3. 关键决策记录

| 决策事项 | 最终结论 | 原因与取舍 | 对实现的影响 |
| --- | --- | --- | --- |
| 详情数据来源 | 复用当前列表项。 | 后端列表已提供详情字段，避免额外请求。 | 详情关闭后不单独缓存资源。 |
| 桌面端详情布局 | 保持首页侧边栏；右侧主体切换为左右双栏，左栏居中展示图片，右栏展示提示词与操作。 | 详情仍属于资产浏览上下文，不离开当前页面；图片周边留白可作为直观关闭区域。 | 不新增详情路由或全屏遮罩；左栏空白区域点击关闭详情，右栏承载关闭、删除等操作且不触发关闭。 |
| 批量操作入口 | 资产页右上角提供“批量操作”按钮；进入后才显示图片复选框、已选数量和“取消选择”。 | 默认点击图片保持预览语义，避免与删除混淆。 | 管理模式中可对手动勾选的图片执行删除或下载。 |
| 批量操作范围 | 本迭代实现删除与下载；收藏和发布可见但不可用，等待迭代 4 接口完成后启用。 | 不为尚未存在的后端能力构造前端状态。 | 删除复用已有批量删除接口；下载仅使用内存中的短期签名 URL。 |
| 批量下载 | 多选下载逐张触发浏览器下载，不生成 ZIP 包。 | 不新增压缩文件生成服务，也避免后端暂存私有资产。 | 用户选择多张图片后依次触发下载；浏览器拦截时提示用户允许多文件下载。 |
| 图片复制 | 复制图片二进制到系统剪贴板；失败时提示用户下载图片。 | 详情操作栏需要直接复用当前图片，不引入链接复制语义。 | 浏览器从 OSS 短期签名 URL 读取 Blob 后写入 Clipboard API；OSS 需允许前端站点 Origin 的 `GET` 跨域请求，无需按用户 IP 配置或新增 Nginx 代理。 |
| 详情右栏信息结构 | 顶栏操作、提示词、生成参数三段纵向排列；右栏整体可滚动，顶栏吸顶。 | 操作随时可达，提示词保持阅读空间，参数不与创作内容混杂。 | 顶栏提供关闭、下载与复制图片；收藏、发布显示为不可用；删除独占危险操作行。 |
| 生成参数数据 | 展示比例、尺寸、生成时间和本次生成数量。 | 为用户保留可追溯的创作配置。 | 当前图片宽高可推导比例，`createdAt` 用于生成时间；资产列表接口返回 `requestedImageCount` 用于展示生成数量。 |
| 资产网格 | 仅适配桌面端窗口宽度，采用 `repeat(auto-fill, minmax(220px, 1fr))` 的自适应列数。 | 宽屏充分利用空间，窄桌面窗口仍保持可识别的卡片尺寸；不扩展到移动浏览器布局。 | 图片使用统一圆角缩略图；悬停显示轻遮罩与生成时间，管理模式才显示复选框。 |
| 删除确认 | 单图与批量删除使用同一确认弹窗。 | 统一不可撤销操作的风险提示。 | 文案为“将永久删除 N 张图片，此操作无法撤销”，操作为“取消 / 删除 N 张图片”。 |
| 日期分组 | 前端按北京时间分组，后端保持 UTC 排序和游标。 | 同时保证用户展示习惯与分页稳定性。 | 追加下一页时合并首个同日期分组。 |
| 删除交互 | 单图与多选复用确认和同一接口。 | 删除语义一致，减少重复链路。 | 成功后从本地列表乐观移除；失败则恢复并提示。 |
| 已删除快照 | 资产页不展示已删除图片。 | 当前页仅展示可管理资产。 | 任务/会话中的无 URL 占位由生成工作台模块处理。 |

## 4. 实施计划与进度

| 小步目标 | 状态 | 完成内容或当前阻塞 | 验证方式/结果 |
| --- | --- | --- | --- |
| 资产页交互设计 | 已完成 | 已确认桌面端网格、详情双栏、批量管理、下载、删除确认与收藏/发布占位。 | 本文第 3、5 节。 |
| API 与领域模型 | 已完成 | `asset-api.ts` 实现分页查询与批量删除；`GenerationAsset` 类型已定义。 | 类型检查和接口联调。 |
| 浏览与详情 | 已完成 | 已实现首次加载、游标追加、空/错状态、URL 刷新与桌面端详情双栏。 | `pnpm lint`、`pnpm build` 通过；OSS CORS 与复制图片已验证，真实后端验收待执行。 |
| 删除管理 | 已完成 | 已实现单图/多选删除、确认弹窗与乐观移除；下载按所选图片逐张触发。 | `pnpm lint`、`pnpm build` 通过；删除与下载联调待执行。 |
| 测试与验收 | 待开始 | 自动化、响应式和真实后端验收。 | `pnpm lint`、`pnpm build` 与联调记录。 |

## 5. 页面、路由与交互设计

路由为 `/assets`，仅认证用户可访问。页面应包含日期分组的图片网格、加载更多入口或等价可访问加载方式、空状态、错误重试、图片详情视图和删除操作。桌面端打开详情后，保留左侧固定导航；右侧主体分为左右两栏，左栏在留白画布中居中展示图片，点击图片周边留白关闭详情。右栏宽度建议固定在 `360px`～`400px`，包含吸顶操作栏、提示词区和生成参数区；右栏内的操作不得触发关闭详情。

| 场景 | 前端行为 |
| --- | --- |
| 首次加载 | 请求第一页；空列表显示“尚无生成图片”及前往生成入口。 |
| 加载下一页 | 使用 `nextCursor` 请求并追加；`null` 表示无更多数据。 |
| 查看详情 | 使用列表项打开详情，展示大图、最终正向提示词及可选负向提示词。 |
| 详情操作栏 | 提供关闭、下载和复制图片；收藏、发布显示为不可用；删除使用独占危险操作行。 |
| 详情参数 | 展示比例、尺寸、生成时间和本次生成数量。 |
| URL 到期 | 不继续使用失效 URL，重新加载当前资产列表后更新内存数据。 |
| 批量操作 | 点击右上角“批量操作”进入管理模式；顶部显示“已选 N 张”和“取消选择”，卡片显示复选框。 |
| 删除单图/多选 | 明确永久删除确认，提交用户选中的 ID；成功后从网格移除。 |
| 下载单图/多选 | 使用当前内存中的签名 URL 下载所选原图；URL 已过期时先刷新当前列表再执行操作。 |
| 图片复制 | 读取当前签名 URL 的图片并写入系统剪贴板；因浏览器权限或 OSS CORS 失败时提示用户下载图片。 |
| 收藏/发布 | 在管理模式中显示为不可用入口；不发送请求，也不模拟收藏或发布状态。 |
| 删除失败 | 保留原项与选择状态，显示安全错误消息并允许重试。 |

本迭代仅实现桌面端 Web；移动浏览器适配与后续单独规划的 React Native App 均不在当前范围。删除按钮不得与图片预览点击区域混淆。具体视觉和组件取舍待实现前确认。

## 6. API、前端模型与状态管理

| 目的 | 接口 | 前端约定 |
| --- | --- | --- |
| 资产分页 | `GET /api/generation-images?cursor=&limit=36` | 使用 `items`、`nextCursor`；响应按 UTC 排序，前端完成北京时间分组。 |
| 删除图片 | `POST /api/generation-images/delete` | 请求为 `{"imageIds":["101","102"]}`；成功 `data` 为 `null`。 |

领域模型至少保留 `imageId`、`url`、`urlExpiresAt`、`width`、`height`、`createdAt`、`finalPrompt`、`finalNegativePrompt`。不得将 URL 存入本地持久化缓存；删除请求不按资源存在性展示差异，以符合后端归属隔离语义。

## 7. 组件与实现位置

> **当前实现状态**：`src/features/assets/ui/assets-workspace.tsx` 已实现资产列表、日期分组、详情、批量管理、逐张下载和删除交互。收藏与发布仅显示不可用占位，等待迭代 4；OSS 前端 Origin CORS 已配置并验证复制图片。

| 职责 | 计划位置 | 说明 |
| --- | --- | --- |
| 路由入口 | `src/app/assets/page.tsx` | 页面组合与认证入口，已渲染资产工作台。 |
| 资产业务 | `src/features/assets/` | API、分页、删除、选择与详情交互（API 层已实现）。 |
| 领域模型 | `src/entities/generation/` | 图片资产及时间/URL 显示规则（类型已定义）。 |
| 通用组件 | `src/shared/` / `src/widgets/` | 图片详情视图和通用反馈组件按复用范围放置。 |

## 8. 异常场景、可靠性与安全

- `40100`、`40102` 统一遵循工程基础文档的认证处理。
- 非法游标或 `limit` 的 `422` 应停止本次加载、保留已加载内容并提示重试；不得自行修改游标猜测下一页。
- 删除接口的“已删除、他人或不存在均成功”是安全约束，UI 不得试图区分资源存在性。
- 签名 URL、提示词和图片不得写入持久化存储、URL、埋点或错误监控；详情页面使用 `Referrer-Policy: no-referrer`。

## 9. 测试与验收

待实施后至少覆盖：空列表、游标追加与跨页日期合并、详情展示、URL 过期刷新、单图/多选删除、重复删除、删除失败恢复和认证失效。执行 `pnpm lint`、`pnpm build` 并使用真实后端完成联调。

## 10. 后续边界

收藏和发布改变的是后续迭代的公开状态，不在本模块预设。公开作品详情可复用图片详情布局，但其提示词可见范围和互动区由社区/发布模块独立设计。
