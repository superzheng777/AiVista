package com.superz.aivista.publication.api;
import com.superz.aivista.common.response.ApiResponse; import com.superz.aivista.common.response.ResponseUtils; import com.superz.aivista.publication.dto.InspirationImageResponse; import com.superz.aivista.publication.service.InspirationQueryService; import java.util.List; import org.springframework.web.bind.annotation.GetMapping; import org.springframework.web.bind.annotation.RequestMapping; import org.springframework.web.bind.annotation.RestController;
@RestController @RequestMapping("/inspirations") public class InspirationController { private final InspirationQueryService service; public InspirationController(InspirationQueryService service){this.service=service;} @GetMapping public ApiResponse<List<InspirationImageResponse>> list(){return ResponseUtils.success(service.list());}}
