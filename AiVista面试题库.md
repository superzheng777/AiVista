# AiVista 项目面试题库（含参考回答）

> 适用方向：校招 / 初级全栈与 Agent 应用开发  
> 题目规模：100 道主问题，每道附 2～4 个连续追问  
> 使用约定：回答采用候选人口述视角，以仓库当前实现为准；涉及后续方案时会明确说明尚未落地

## 1. 标签说明

- **优先级**：`P0` 为简历上最容易被问到的必问题；`P1` 为高频追问；`P2` 为压力面或方案深挖。
- **来源类型**：
  - `面经原型`：按照真实第一人称面经中出现过的提问角度，结合 AiVista 改写。
  - `简历直接追问`：面试官看到简历中的技术表述后，最自然的项目追问。
  - `八股延伸`：从项目实现继续下钻的计算机基础、框架机制或中间件知识。
- **模块标签**：`前端`、`Java 服务端`、`Agent Worker`、`架构`、`优化`，一道题可以属于多个模块。
- **来源编号**：每题只标注对应的面经组；来源只证明提问角度，不表示原面经出现过 AiVista 的具体实现。

## 2. 调研来源

### 2.1 第一人称面经（主要来源）

- **S1｜字节大模型应用开发一面**：项目完整流程、Agent 核心模块、Tool、Memory、ReAct / Plan-Execute、循环终止与执行安全。[原文](https://www.nowcoder.com/discuss/916420927619928064)
- **S2｜网易 / 阿里 / 腾讯 Agent 后端面试复盘**：框架选型、Tool 输出校验、失败案例、并发 Agent 会话、MVCC、线程池与故障排查。[原文](https://www.nowcoder.com/discuss/919608103723622400)
- **S3｜字节 AI 全栈开发一面**：从前端 SSE 到后端 Agent 的完整链路、多模态输入、会话 ID、框架取舍以及 AI 辅助编码边界。[原文](https://www.nowcoder.com/discuss/913848724952997888)
- **S4｜七牛云后端开发实习面经**：项目架构、数据表、MQ 消息体、消息丢失与重复、ACK、RabbitMQ / Kafka 选型。[原文](https://www.nowcoder.com/discuss/353156401826570240)
- **S5｜杭州端点 Java 实习面经**：接口防重、可靠 MQ、MySQL 锁与隔离级别、MVCC、Java 线程池与 JUC。[原文](https://ac.nowcoder.com/discuss/663855)
- **S6｜知乎前端面经**：项目职责、SSR / CSR、SSE、生成失败、弱网图片列表、事件循环、状态管理和前端安全。[原文](https://www.nowcoder.com/discuss/892108187820650496)
- **S7｜字节国际化广告前端一面**：项目用户与背景、刷新后状态恢复、SSE / WebSocket 选型、心跳与指数退避。[原文](https://www.nowcoder.com/discuss/888046680824639488)
- **S8｜Java 日常实习面经**：WebSocket 与 HTTP、SSE 与 WebSocket、网络传输、数据库事务与隔离级别。[原文](https://www.nowcoder.com/discuss/592132499082317824)

### 2.2 汇总材料（只用于交叉验证频率）

- **C1｜腾讯 / 百度大模型与 Agent 面经汇总**：Agent 编排、Skill、工具调用、失败恢复、模型选型、评测、数据库与 MQ。[原文](https://www.nowcoder.com/discuss/878600528970735616)
- **C2｜前端高频面试题汇总**：SSE / WebSocket、流式 Markdown、心跳重连、浏览器与 React 基础。[原文](https://www.nowcoder.com/discuss/889546972099584000)
- **C3｜Agent 简历与面试复盘**：项目真实性、框架改造、性能证据、失败边界和搜索索引陈旧数据。[原文](https://www.nowcoder.com/discuss/924971145856569344)

### 2.3 项目事实基线

题目中的项目事实以 [项目 README](README.md)、[Agent 模式模块](backend/aivista/Agent模式模块.md)、[TypeScript AI Runtime](backend-ts/README.md)、[图像生成任务模块](backend/aivista/图像生成任务模块.md)、[图像访问与分发优化模块](backend/aivista/图像访问与分发优化模块.md)、[关键词搜索模块](backend/aivista/关键词搜索模块.md) 和 [点赞系统优化方案](backend/aivista/点赞系统优化.md) 为准。

Pi Agent SDK 相关题目按项目使用的 `@earendil-works/pi-coding-agent 0.83.0` 核对，特别区分 `session.subscribe` 与扩展层 `pi.on`、`agent_end` 与 `agent_settled`。

---

## 3. 项目真实性与开场（8 题）

### Q001｜P0｜面经原型｜架构

**主问题：** 先介绍一下 AiVista，这个项目解决什么问题，你在里面具体负责哪些部分？

**连续追问：** ① 最初为什么想做这个项目？ ② 如果只用一句话概括它与普通 AI 生图 Demo 的区别，你会怎么说？

**参考回答：** AiVista 是我独立完成的 AI 图像创作平台，目标不是只接一个生图接口，而是把灵感浏览、Agent 创作、资产管理和社区发布串成完整流程。我负责产品设计、Next.js 前端、Java Core 和 TypeScript AI Runtime。最初做它，是因为普通 Demo 只展示“能生成”，没有处理长任务、失败恢复、权限和资产沉淀。它和普通生图 Demo 的区别，可以概括为：生成只是中间步骤，前后还有可靠执行、实时反馈和可管理的业务闭环。

**来源：** S4、S7

### Q002｜P0｜简历直接追问｜前端 / Java 服务端 / Agent Worker / 架构

**主问题：** 画出一次 Agent 创作从用户提交请求到图片出现在前端的完整链路。

**连续追问：** ① 每个组件分别保存什么状态？ ② 哪些通道承载命令，哪些通道只承载实时事件？

**参考回答：** 浏览器先通过 REST 向 Java Core 创建 Agent Creation。Java 完成鉴权、约束校验和事务落库，同时写 Outbox；Dispatcher 再把最小执行命令发到 RabbitMQ。TypeScript Worker 消费命令，读取 Java 的权威快照，创建短生命周期 Pi Session，并通过 Tool 复用普通生成任务。生成结果先落 OSS，再由 Worker 通过幂等 Completion 接口交给 Java 原子确认资产和终态。Agent 实时事件走 Worker 到 Java 的 WebSocket，再由 Java 用 SSE 推给浏览器；断线后前端以 REST 快照恢复。MySQL 中的业务状态由 Java 持有，Worker 只维护自己的执行 Ledger。RabbitMQ 承载执行命令，WebSocket 和 SSE 只承载实时事件与控制信号。

**来源：** S1、S3

### Q003｜P0｜面经原型｜架构

**主问题：** 这个项目目前真正完成到什么程度，哪些能力已经联调，哪些还只是后续方案？

**连续追问：** ① 有没有真实 Provider 的端到端验证？ ② CDN、多实例和生产监控目前分别是什么状态？

**参考回答：** 已完成的是用户认证、普通文生图和图生图、Agent 模式、Outbox 到 RabbitMQ 的执行链路、幂等 Completion、实时事件、取消、资产与社区核心流程。真实百炼模型的文本流、Tool Result 后续 Turn、WebP 图片输入，以及文生图和图生图的本地端到端链路都做过验证。当前 Java 和 Worker 仍按单实例边界运行；CDN 私有源站、完整监控告警、备份恢复和生产部署还在路线图中。我会把这些明确说成后续演进，不会当作已经上线的成果。

**来源：** S1、C3

### Q004｜P0｜简历直接追问｜架构

**主问题：** 简历写了 Agent 创作、可靠执行、实时交互和资产闭环，其中你认为最难的一项是什么？

**连续追问：** ① 难点来自业务还是技术？ ② 最初方案和最终方案有什么变化？

**参考回答：** 我认为最难的是可靠执行，因为它同时涉及本地事务、消息投递、外部模型副作用、Worker 崩溃和最终业务状态。最初容易把流程理解成“消费消息、调用模型、回传结果”，但这样无法回答调用成功后崩溃、响应未知或重复投递怎么办。后来我把它拆成 Java 事务 Outbox、RabbitMQ 至少一次投递、Worker Ledger 与检查点、确定性对象 Key，以及 Java 幂等 Completion。难点不只是中间件用法，而是为每个崩溃窗口定义可恢复行为。

**来源：** S2、S6

### Q005｜P1｜面经原型｜架构 / 优化

**主问题：** 这是个人项目还是团队项目，有没有真实用户、线上流量或可量化指标？

**连续追问：** ① 如果没有生产流量，你用什么方法验证可靠性设计？ ② 哪些结论不能因为本地测试通过就直接写进简历？

**参考回答：** 这是个人项目，目前没有可以对外宣称的生产用户量或线上 SLA。我主要通过单元测试、集成测试、Faux Provider、真实 Provider 冒烟和完整本地 E2E 验证实现，还会针对重复消息、取消后迟到写回、同一幂等键重复请求等场景做定向测试。这能证明流程按预期工作，但不能证明高并发容量、生产可用性或真实用户效果，所以我不会写“支撑百万请求”“线上零故障”这类没有数据的结论。

**来源：** C3

### Q006｜P1｜面经原型｜Java 服务端 / Agent Worker

**主问题：** 讲一个开发过程中真实遇到的失败场景，你是怎么发现和定位的？

**连续追问：** ① 当时有哪些错误假设？ ② 最终增加了什么测试或观测手段防止复发？

**参考回答：** 一个典型问题是我最初容易把“Agent 已经结束”理解成收到 `agent_end`，但 Pi 在重试或压缩后可能再次进入执行，过早收尾会造成重复 Completion。核对 SDK 事件语义后，我改用每个 prompt 稳定触发一次的 `agent_settled` 做最终收口，并保留 Tool 和消息事件用于过程投影。随后增加了 Faux Provider 测试和真实模型冒烟，验证 Tool Call、Tool Result、后续 Turn 与单次 settled 的完整顺序。

**来源：** S2、S3

### Q007｜P1｜简历直接追问｜前端 / Java 服务端 / Agent Worker

**主问题：** 你个人写了哪些核心代码，哪些能力来自框架、中间件或云服务？

**连续追问：** ① 如果去掉 Pi Agent SDK，你还需要自己实现哪些部分？ ② RabbitMQ、OSS 和百炼分别替你解决了什么？

**参考回答：** 我自己设计并实现了 Java 与 Worker 的职责边界、版本化协议、Outbox、执行 Ledger、Completion 幂等、Tool 适配、事件归一化、取消链路和资产权限。Pi SDK 提供会话、Agent Loop、模型与 Tool 调用及事件机制；如果去掉它，我还要实现多轮循环、消息状态、Tool 调度、重试和上下文处理。RabbitMQ负责可靠传递执行命令，OSS负责对象存储，百炼提供模型能力，但它们都不会替我处理业务事务、权限、幂等和最终一致性。

**来源：** S3

### Q008｜P2｜面经原型｜架构

**主问题：** 如果面试官现场打开仓库，你会选择哪三处代码证明简历中的四项描述？

**连续追问：** ① 为什么这三处最能体现你的工作？ ② 如果被问到一段 AI 辅助生成的代码，你怎样证明自己理解过？

**参考回答：** 我会先展示 Java 的任务创建与 Outbox 事务，证明可靠派发；再展示 TypeScript 的 Agent Runtime、Tool 和执行 Ledger，证明 Agent 不是简单转发模型；最后展示 Java Completion 与资产落库，说明状态如何闭环。AI 确实参与过局部编码和测试草稿，但架构边界、失败矩阵和协议是我设计的。我会从状态转换、事务边界和测试用例解释代码，并能现场追踪一次请求，这比强调“是不是逐行手写”更能证明理解程度。

**来源：** S3、C3

---

## 4. Agent 创作（24 题）

### Q009｜P0｜简历直接追问｜Agent Worker / 架构

**主问题：** 为什么选择 Pi Agent SDK，而不是 LangChain、LangGraph、Spring AI，或者自己实现 Agent Loop？

**连续追问：** ① 选型时比较了哪些维度？ ② 如果框架升级出现破坏性变化，你准备怎么控制风险？

**参考回答：** 我选择 Pi Agent SDK，主要因为它提供了比较薄的事件驱动 Runtime，可以直接控制 Session、Tool、资源加载和生命周期，不需要把业务状态塞进框架自己的工作流抽象。AiVista 的任务、权限和资产事实都在 Java，所以我更需要可嵌入的 Loop，而不是再引入一套状态图。相比 Spring AI，Pi 的 TypeScript 生态和当前 Tool 接线更直接；相比自己写循环，它省掉了模型适配、Tool 调度和事件细节。项目锁定 0.83.0，不跟随 latest；升级时先对照 changelog、类型声明和现有冒烟测试，再决定是否迁移。

**来源：** S1、S2、S3

### Q010｜P0｜简历直接追问｜Agent Worker

**主问题：** 简历中的“Skill 驱动”具体体现在哪里，新增一个 Skill 需要经过哪些步骤？

**连续追问：** ① Skill 与系统提示词、Tool 的职责有什么区别？ ② 当前为什么只有 `poster-design` Skill？

**参考回答：** Skill 在这里是一份按需加载的领域创作方法，例如海报任务要怎样澄清信息、组织视觉层级和使用生成工具。系统提示词定义 Agent 的全局身份与硬约束，Tool 提供可执行能力，Skill 则告诉模型在特定任务里如何组合这些能力。Runtime 启动时只暴露 Skill 索引，模型需要时再通过受限 `read` 读取正文。新增 Skill 主要是增加规范文件、更新可见索引并补测试，不需要修改 Tool 协议。当前只做 `poster-design`，是为了先把一条垂直链路验证完整，避免用大量未经验证的 Skill 制造“能力很多”的假象。

**来源：** C1、S6

### Q011｜P0｜面经原型｜Agent Worker

**主问题：** Agent 收到用户请求后，怎么决定调用 Tool，还是直接生成最终回复？

**连续追问：** ① Tool Result 回到模型后为什么还可能继续下一轮？ ② 没有调用 Tool 但输出了文字算成功还是失败？

**参考回答：** 决策由模型根据系统提示词、当前上下文和 Tool Schema生成。Pi 收到 Tool Call 后执行对应 Tool，把结构化结果作为 Tool Result 放回上下文，模型再进入下一 Turn，所以它可以继续生成说明、再次调用 Tool，或者结束。AiVista 不把“必须调用 Tool”写死在 Harness 中；没有 Tool Call 但有非空最终回复，也是一种正常成功，例如用户只是询问创作建议。真正的硬边界由白名单、权限、最大 Turn、时间和数量约束控制。

**来源：** S1、C1

### Q012｜P0｜简历直接追问｜Agent Worker / 架构

**主问题：** 你设计的统一 Tool 注册与执行协议到底统一了什么？

**连续追问：** ① Tool 的参数、结果、错误和进度分别怎样表示？ ② 业务工具与 SDK ToolDefinition 之间如何适配？

**参考回答：** 我把统一协议分成定义层和执行层。定义层通过 `defineTool` 和 TypeBox 描述名称、用途及参数结构；执行层统一接收 `toolCallId`、业务请求和 `AbortSignal`，返回带资产 ID、任务状态或失败码的结构化结果。进度事件用同一个 `toolCallId` 关联开始、进度和结束。具体的 `text_to_image`、`image_to_image` 只负责参数转换和结果表达，真正的生成由 `AgentGenerationToolExecutor` 创建 Java Generation Task 并等待权威完成结果。这样以后增加 Tool 时，不需要重新设计取消、观测和错误约定。

**来源：** S1、S2

### Q013｜P0｜八股延伸｜Agent Worker / Java 服务端 / 安全

**主问题：** Tool 参数已经通过 JSON Schema 校验，但图片并不属于当前用户，为什么仍然不能执行？

**连续追问：** ① 结构校验、语义校验和权限校验应该分别放在哪里？ ② 为什么不能信任模型生成的资产 ID？

**参考回答：** JSON Schema 只证明参数形状正确，例如字段存在、类型正确、数量在范围内；它不能证明资产存在，更不能证明属于当前用户。Tool 层先用本次 Creation 的授权资产集合做语义和权限检查，Java 创建生成任务时还会再按当前用户校验一次。模型输出本质上是不可信输入，资产 ID 可能来自幻觉、提示词注入或上下文污染，所以不能因为它通过 Schema 就直接访问 OSS。双层校验能避免 Worker 逻辑遗漏后越过业务权限边界。

**来源：** S1、S2

### Q014｜P0｜面经原型｜Agent Worker

**主问题：** Tool 返回字段缺失、格式变化、结果过大或模型无法理解时，你会怎么处理？

**连续追问：** ① 哪些错误允许 Agent 修正参数后重试？ ② 哪些错误应该直接终止本轮执行？

**参考回答：** Tool 不会把任意异常堆栈原样塞给模型，而是映射成稳定的结构化结果。参数格式、数量超限这类可修正错误可以明确告诉模型约束，让它调整后再调用；权限拒绝、资源不存在、任务被取消或执行预算耗尽则不应让模型反复尝试。外部服务的临时故障是否重试，要看请求是否确定没有发出以及副作用是否可判定。对于过大的结果，我只投影模型真正需要的资产 ID、状态和简短说明，完整业务状态仍保留在服务端。

**来源：** S1、S2

### Q015｜P0｜简历直接追问｜Agent Worker / 架构

**主问题：** 运行时 Harness 具体控制哪些事情，它和 Pi SDK 自带能力的边界在哪里？

**连续追问：** ① 哪些限制必须由确定性代码执行？ ② 为什么不能只在 Prompt 里要求模型遵守限制？

**参考回答：** Harness 是我放在 Pi Runtime 外围的确定性控制层，负责最大 20 Turn、执行时间、可用 Tool、并发、授权资源、生成数量和取消传播。Pi SDK 负责 Session、模型交互、Tool 调度和事件生命周期。Prompt 可以帮助模型理解规则，但不能作为安全或计费边界，因为模型可能忽略、误解或被用户输入干扰。权限、额度、最大调用范围和中止必须由代码检查，模型只能在允许的范围内做选择。

**来源：** S2、C1

### Q016｜P0｜面经原型｜Agent Worker / 优化

**主问题：** 怎样防止 Agent 无限循环、重复调用同一个 Tool 或者持续消耗 Token？

**连续追问：** ① 只限制最大 Turn 数有什么不足？ ② 合法轮询和无进展循环怎么区分？

**参考回答：** 当前项目已经有 20 Turn 的硬上限和总执行时间限制，先保证任何 Creation 最终会停下来。但只看 Turn 不够，因为重复的 Tool 调用仍可能在上限前制造多次外部副作用。进一步可以对 Tool 名称和规范化参数计算指纹，并结合结果状态判断是否有进展。合法轮询应该看到任务版本或状态发生变化，而且有明确 deadline；相同输入、相同结果连续出现且没有新信息，才更像无进展循环。对生成 Tool 还要结合稳定幂等键，不能靠“再试一次”碰运气。

**来源：** S1、S2

### Q017｜P0｜简历直接追问｜Agent Worker

**主问题：** 为什么一次 Creation 创建一个短生命周期的内存 Pi Session，而跨轮会话保存在 MySQL？

**连续追问：** ① 为什么不直接使用 Pi 默认的 JSONL 会话？ ② Worker 重启后如何继续为用户展示历史消息？

**参考回答：** Pi 的物理 Session 只服务一次 Creation，执行结束就释放；用户跨轮看到的 Generation Session 才是业务会话。这样做是因为 Pi 默认 JSONL 更适合单用户 CLI，不适合多用户 Web 的权限、数据生命周期和查询方式。Java 把消息、Creation、Activity 和任务快照持久化到 MySQL，下一次执行时 Worker 只装载最近 12 条、最多 12000 code point 的纯文本历史。Worker 重启不会丢用户记录，前端也始终从 Java 的 REST 快照恢复，而不是依赖某个进程内 Session。

**来源：** S3

### Q018｜P1｜八股延伸｜Agent Worker

**主问题：** `session.subscribe` 和扩展层 `pi.on` 有什么区别，项目为什么同时用到它们？

**连续追问：** ① 哪些事件只能从扩展层获取？ ② 扩展事件处理器里执行慢 I/O 会带来什么问题？

**参考回答：** `session.subscribe` 是外部订阅层，适合接收消息、Turn、Tool 执行和 `agent_settled` 等运行事件；`pi.on` 是扩展层，还能处理 `context`、`tool_call`、`tool_result`、`before_agent_start`、`input` 和 `model_select` 这六类扩展独有事件。AiVista 用内联扩展做 Turn 级 Harness 控制，用 subscribe 做事件归一化和对外投影。`pi.on` 的处理器会被执行链 await，如果里面同步做慢数据库 I/O，会直接拖慢 Agent Loop，所以这类工作要放到异步队列，或者在 subscribe 能覆盖时优先在那里处理。

**来源：** C1

### Q019｜P1｜简历直接追问｜Agent Worker / 前端

**主问题：** 你为什么用 `agent_settled` 判断本轮稳定结束，而不是只监听 `agent_end`？

**连续追问：** ① retry 或 compaction 会怎样影响结束事件？ ② 怎样保证每个 prompt 只进行一次最终收尾？

**参考回答：** 在 Pi 0.83.0 中，`agent_end` 表示一轮 Agent 结束，但重试、压缩或队列处理后可能再次进入执行，因此它不适合作为唯一的业务收尾信号。`agent_settled` 会在这些内部过程都稳定后触发，并且每个 prompt 一次。项目用它结束事件订阅、整理最终消息并进入 Completion，同时 Java 的 Completion 接口还以稳定 ID 做幂等。这样即使上游事件重复，业务终态也只会收敛一次。

**来源：** C1

### Q020｜P1｜面经原型｜Agent Worker

**主问题：** 当前项目的 Memory 是怎么设计的，为什么没有直接加入向量数据库和长期记忆？

**连续追问：** ① 对话历史、任务状态和用户长期偏好是不是同一种数据？ ② 最近 12 条消息的窗口可能丢掉哪些信息？

**参考回答：** 当前实现只有受控的短期对话记忆：Java 持久化完整业务消息，Worker 每次装载最近 12 条、最多 12000 code point 的纯文本历史。任务状态由结构化表和快照保存，不混进自然语言 Memory；用户长期偏好暂时没有实现。这样更容易保证权限和可恢复性，也避免未经确认的模型总结变成长期事实。它的不足是较早的约束可能被窗口截掉，所以如果对话变长，我会优先增加结构化摘要字段，而不是直接上向量库。

**来源：** S1、S2

### Q021｜P1｜简历直接追问｜Agent Worker / 安全

**主问题：** Skill 采用“索引到受限 read 再到正文”的渐进披露链路，为什么这样设计？

**连续追问：** ① 如果把所有 Skill 正文一次性塞入上下文会怎样？ ② 怎样阻止 `read` 越过 `.pi/skills`？

**参考回答：** 渐进披露让模型先看到很短的 Skill 索引，只有判断任务相关时才读取正文。这样能控制上下文长度，也减少无关说明对 Tool 选择的干扰。Pi 内置 `read` 本身不提供业务需要的路径白名单，所以项目没有直接开放任意文件读取，而是包装了受限读取能力：先解析和规范化路径，再校验最终路径必须位于 `.pi/skills` 下。系统提示词只是提醒，真正的目录边界仍由代码执行。

**来源：** S6、C1

### Q022｜P1｜面经原型｜Agent Worker / 优化

**主问题：** 当会话越来越长时，你如何判断应该截断、滑动窗口、摘要还是检索历史？

**连续追问：** ① 直接按字符截断可能破坏哪些约束？ ② 摘要错误怎样避免持续污染后续对话？

**参考回答：** 当前阶段采用最近消息窗口和总 code point 上限，优点是行为简单、容易验证；缺点是早期确认过的画幅、数量或设计偏好可能被截掉。直接从中间截断还可能破坏消息角色和 Tool Call、Tool Result 的配对。后续如果加入摘要，我会把已确认约束、已生成资产和未完成事项做成结构化字段，并保留原消息引用。摘要不能覆盖数据库里的业务事实，也要允许重新生成和纠错，避免一次模型误判长期污染会话。

**来源：** S1、S6

### Q023｜P1｜面经原型｜Agent Worker / 架构

**主问题：** AiVista 当前属于 ReAct、Plan-Execute、固定工作流还是混合模式？

**连续追问：** ① 海报设计为什么适合或不适合先规划再执行？ ② 什么情况下你会把模型决策改成确定性工作流？

**参考回答：** AiVista 当前更接近受约束的 ReAct：模型根据用户意图读取 Skill、调用生成 Tool、观察结果，再决定继续还是回复；没有单独持久化完整 Plan，也不是多 Agent 编排。海报创作有探索性，生成结果会影响下一步，所以完全固定的流程不够灵活。但审核、权限校验、额度和资产落库都是确定性工作流。如果任务步骤固定、合规要求高，或者模型试错成本过高，我会把模型限制在某个节点，只让它生成参数，不让它决定整个业务流程。

**来源：** S1、S2

### Q024｜P1｜八股延伸｜Agent Worker / 安全

**主问题：** 多模态请求中的图片内容、图片 URL 和资产 ID 分别属于哪一层信息？

**连续追问：** ① 为什么 Data URI 不应该被持久化到消息历史？ ② 模型看到图片内容后为什么仍需要准确的资产 ID 绑定？

**参考回答：** 图片二进制由 Worker从已授权 OSS 对象读取，在当前请求中组装成 Pi 的 `ImageContent`，只用于模型感知；资产 ID 是业务引用，用于 Tool 参数和后续权限校验。Data URI 体积大、重复存储成本高，也容易把敏感内容带进日志和会话历史，所以不会持久化。模型即使看到了图片，也无法可靠猜出内部 ID，因此 Runtime 会在本轮系统上下文中按顺序绑定准确资产 ID。随后 Tool 和 Java 仍会做授权校验。

**来源：** S3

### Q025｜P1｜面经原型｜Agent Worker / 优化

**主问题：** 如果 Tool 数量从 2 个增长到 100 个，工具描述导致上下文过长和选错工具，你会怎么改架构？

**连续追问：** ① Tool 分组、检索和动态白名单分别解决什么问题？ ② 工具选择错误如何评测？

**参考回答：** 如果增长到 100 个 Tool，我不会把全部 Schema 每轮都塞给模型。可以先按领域分组，用意图或轻量检索选出候选集合，再根据用户权限和当前任务生成动态白名单；真正执行时仍走统一协议。分组降低选择空间，检索减少 Prompt，白名单负责安全边界。评测上会准备带标准 Tool 与参数的任务集，分别统计候选召回、最终选 Tool、参数合法率和任务成功率，还要加入相似工具、无须调用 Tool 和越权请求这些负例。

**来源：** S1、C1

### Q026｜P1｜简历直接追问｜Agent Worker / Java 服务端

**主问题：** `text_to_image` 和 `image_to_image` 为什么不是直接调用模型，而是创建普通 Generation Task？

**连续追问：** ① 这样复用了哪些已有能力？ ② Tool 等待生成完成时如何被同进程 Coordinator 唤醒？

**参考回答：** 两个 Tool 不直接调用百炼，是为了复用普通 Generation Pipeline 已有的额度、幂等、MQ、Ledger、OSS 转存、资产创建和失败分类。Tool 通过受保护的 Java 内部接口创建 Generation Task，并使用由 Creation Task ID 和 `toolCallId` 派生的稳定幂等键。独立 Generation Consumer 完成任务后，Java 先原子确认 Completion；确认成功后，同进程 `GenerationCompletionCoordinatorService` 唤醒等待中的 Tool。Tool 拿到的是 Java 已确认的资产 ID，而不是 Worker 自己推测的结果。

**来源：** S2

### Q027｜P1｜面经原型｜Agent Worker / 优化

**主问题：** 如何评估 Agent 的 Tool 选择、参数填写、任务完成和最终图片质量？

**连续追问：** ① 离线评测集怎么构造？ ② 同一个提示词的生成结果具有随机性时怎么比较？

**参考回答：** 当前项目还没有可以声称的线上 Agent 指标，所以我会把评测拆开。Tool 层可以用确定性样本统计是否选对 Tool、参数是否合法、越权是否被拦截、是否在预算内结束；流程层统计 Completion 成功率、取消收敛和恢复能力；图片质量再用人工维度或视觉模型做辅助评分。生图有随机性，不能逐像素比较，我会固定输入和模型配置，多次采样后比较构图、文本可读性、风格遵循和失败率，并保留人工复核。

**来源：** S1、C1

### Q028｜P2｜八股延伸｜Agent Worker

**主问题：** 如果多个 Tool 并行执行，怎样根据 `toolCallId` 关联开始、进度和结束事件？

**连续追问：** ① 为什么不能只依赖事件到达顺序？ ② 某个 Tool 先完成但对应文本后到达时前端如何组织状态？

**参考回答：** 并行 Tool 的事件可能交错，网络转发和批量合并也可能改变到达时序，所以不能用“最近开始的 Tool”推断归属。每次调用都有稳定的 `toolCallId`，Normalizer 和前端都以它作为状态键，分别更新开始、进度和结束。页面可以按首次出现顺序展示活动卡片，但状态合并按 ID 完成。文本流和 Tool 状态是两种投影，不要求严格同步；最终 REST 快照再提供稳定结果。

**来源：** S2

### Q029｜P2｜面经原型｜Agent Worker / 安全

**主问题：** 用户输入中包含提示词注入，诱导 Agent 读取其他 Skill 路径或访问他人图片时，边界怎么守？

**连续追问：** ① System Prompt 能否构成真正的安全边界？ ② Tool 层和 Java 服务端为什么还要做双层授权？

**参考回答：** System Prompt 只能降低模型产生危险动作的概率，不能当安全边界。项目只开放生成 Tool 和受限 Skill 读取，不开放 bash、任意 read、edit 或 write。用户图片先由 Java 生成授权快照，Tool 只接受快照内的资产 ID，Java 创建任务时再按当前用户检查一次。即使提示词注入让模型尝试构造路径或他人 ID，确定性校验也会拒绝。双层授权可以防止 Worker 适配层出错后直接越过核心业务权限。

**来源：** S1、S6

### Q030｜P2｜面经原型｜Agent Worker / 架构

**主问题：** 为什么当前不使用多 Agent，什么场景才值得拆成规划、生成和审查 Agent？

**连续追问：** ① 多 Agent 会增加哪些通信和一致性成本？ ② 如何避免多个 Agent 相互推诿或重复工作？

**参考回答：** 当前需求只有一条图像创作链路，单 Agent 加两个生成 Tool 已经足够。拆成规划、生成和审查 Agent 会增加模型调用、上下文传递、延迟、费用和状态恢复难度，而且各 Agent 的责任边界还没有数据证明必要。如果以后任务真的包含独立且可并行的专业环节，我会让它们通过结构化任务状态协作，每个 Agent 有明确输入、输出和截止条件，由确定性调度器分派，避免自由聊天式协商和相互推诿。

**来源：** S1、S3

### Q031｜P2｜简历直接追问｜Agent Worker / Java 服务端

**主问题：** 用户中止 Agent 时，模型推理、Pi Loop、Tool 等待和外部图片生成分别能否真正取消？

**连续追问：** ① `session.abort()` 与 Tool 的 `AbortSignal` 分别作用在哪里？ ② 外部生成已经完成但 Agent 被取消时，产物应该如何处理？

**参考回答：** Java 先在事务里把 Creation 标记为取消并提升版本，再通过内部 WebSocket 向执行实例发送控制帧。Worker 调用 `session.abort()` 中止 Pi 当前 prompt，同时把 `AbortSignal` 传入 Tool，停止本地等待和可取消的 I/O。但已经到达外部 Provider 的生成不一定能撤回，所以取消不等于保证不产生图片。普通 Generation Task 仍按自己的可靠链路收敛，生成物可以成为用户资产，但 Agent 不再把它写入已取消 Creation 的最终回复；Java 的版本和状态检查会拒绝过期写回。

**来源：** S2

### Q032｜P2｜面经原型｜Agent Worker / 优化

**主问题：** 如果 Agent 平时一分钟完成，某次运行十五分钟还没有结束，你会从哪些层次排查？

**连续追问：** ① 怎样区分模型变慢、Tool 变慢和循环次数增加？ ② 为了支持这种排查，需要记录哪些可观测数据？

**参考回答：** 我会先按阶段拆耗时：排队时间、模型首 Token、每个 Turn、每次 Tool、普通生成任务以及 Completion。Turn 数突然增加通常指向循环或 Prompt 变化；单次模型耗时上升看 Provider；Tool 等待长则继续下钻到 MQ、生成和 OSS。需要用同一个 Creation ID、Task ID 和 `toolCallId` 串起日志，并记录状态转换时间、模型调用次数、Token、失败码和取消时间。当前生产级监控还没完成，这是上线前必须补的部分。

**来源：** S2、C1

---

## 5. 可靠执行（24 题）

### Q033｜P0｜简历直接追问｜Java 服务端 / Agent Worker / 架构

**主问题：** 为什么图片生成必须异步化，不使用 RabbitMQ 可以有哪些实现方案？

**连续追问：** ① 同步 HTTP 一直等待会占用哪些资源？ ② 数据库任务表加轮询能否替代 MQ？

**参考回答：** 生图调用可能持续几十秒甚至更久，如果让创建接口同步等待，会长期占用 HTTP 连接、服务端线程和网关超时预算，用户刷新后还难以恢复。AiVista 的创建接口只负责鉴权、额度和事务落库，随后通过 RabbitMQ 把执行交给独立 Worker。数据库任务表加轮询也能做，但需要自己处理高频扫描、抢占、唤醒延迟和失败恢复。项目已经需要路由、背压和手动 ACK，所以 RabbitMQ 更合适，MySQL 仍负责权威任务状态。

**来源：** S4

### Q034｜P0｜面经原型｜Java 服务端 / 架构

**主问题：** 为什么选择 RabbitMQ，而不是 Kafka、RocketMQ 或 Redis Stream？

**连续追问：** ① 这个业务更关注吞吐、延迟、路由还是消费语义？ ② 如果改用 Kafka，任务重试和消费顺序会有什么变化？

**参考回答：** 这里更看重任务级路由、低延迟、手动 ACK、独立并发池和方便的失败处理，而不是超大吞吐日志流。RabbitMQ 的 Direct Exchange、Quorum Queue、prefetch 和消费者确认与任务队列模型比较贴合。Kafka 更适合可重放的高吞吐事件流，但分区顺序、offset 提交和失败任务处理方式不同，单个长任务还可能影响分区消费。RocketMQ 也能完成，但项目体量下引入另一套更重的运维没有明显收益；Redis Stream 则需要自己补更多可靠投递和治理细节。

**来源：** S4、S5

### Q035｜P0｜面经原型｜Java 服务端 / Agent Worker

**主问题：** 从用户提交任务到 Java 确认终态，哪些位置可能丢消息，哪些位置可能产生重复？

**连续追问：** ① 生产端、Broker、消费端和结果提交端分别有什么崩溃窗口？ ② 哪些重复可以靠幂等消除，哪些外部副作用无法简单撤销？

**参考回答：** 生产端可能在数据库提交后尚未发 MQ，或消息已发但 Outbox 未标记；Broker 可能路由失败；消费者可能在 ACK 前后崩溃；Worker 还可能在模型调用、OSS 上传、Ledger 写入和 Completion 前后退出。前几类重复可以靠 Outbox 状态、消息重投、确定性对象 Key、Ledger 和幂等 Completion 收敛。最棘手的是模型请求已经发出但响应未知，因为它可能已经计费并产生图片，不能无脑再调一次。这个窗口只能保守终止、等待人工或依赖 Provider 提供的幂等查询能力。

**来源：** S4、S5

### Q036｜P0｜简历直接追问｜Java 服务端 / 架构

**主问题：** 事务 Outbox 解决的是哪两个操作之间的一致性问题？

**连续追问：** ① 为什么不能在数据库提交后直接发送一次消息？ ② Outbox 是否能保证消息只发送一次？

**参考回答：** 创建任务和写 Outbox 在同一个 MySQL 事务里完成，解决“业务已经提交但执行命令没留下可靠记录”的问题。如果先提交数据库再直接发 MQ，进程在两步之间崩溃，任务会永久停留；如果先发消息再提交，Worker 可能处理一个最终回滚的任务。Outbox 不保证只发一次，Dispatcher 在发送成功但标记前崩溃仍会重复，所以它提供的是可重试发布，消费者仍要按至少一次和幂等处理设计。

**来源：** S4、S5

### Q037｜P0｜八股延伸｜Java 服务端

**主问题：** 业务事务已经提交，但 Outbox Dispatcher 还没有发布消息，此时系统如何继续推进？

**连续追问：** ① Dispatcher 扫描条件和领取状态应该怎么设计？ ② Dispatcher 进程长期不可用时用户会看到什么状态？

**参考回答：** Dispatcher 扫描到期的 PENDING 事件，用条件更新把事件领取为处理中，再发布到 MQ；失败则记录原因和下次可用时间。领取还要带租约，实例崩溃后，超过租约的事件可以重新排队。业务事务已经提交，所以用户仍能从 REST 看到 QUEUED 任务，只是迟迟不执行。系统需要通过最老 Outbox 年龄、积压量和排队超时发现问题，不能把“任务已创建”误认为“命令已经成功交给 Worker”。

**来源：** S5

### Q038｜P0｜八股延伸｜Java 服务端 / RabbitMQ

**主问题：** 消息已经发到 RabbitMQ，但 Dispatcher 在标记 Outbox 已发送前崩溃，会发生什么？

**连续追问：** ① Publisher Confirm 能否消除这个重复窗口？ ② 为什么消费者仍必须按至少一次投递设计？

**参考回答：** 这个窗口会导致同一 Outbox 事件再次发送。Publisher Confirm 只能告诉生产者 Broker 是否接收了发布，不能把“Broker 确认”和“MySQL 标记已发送”合成一个原子事务。因此确认返回后、数据库更新前仍可能崩溃。消费者必须接受重复命令：先根据业务任务和 Worker Ledger 判断是否已经取得执行权或已有结果，再决定执行、复用或 ACK，而不是假设每条消息只来一次。

**来源：** S4、S5

### Q039｜P0｜面经原型｜Java 服务端 / RabbitMQ

**主问题：** Publisher Confirm、Mandatory Return 和消费者 ACK 分别发生在哪一段链路？

**连续追问：** ① Exchange 找不到匹配队列时哪个机制能够发现？ ② 消费者什么时候 ACK 才不会过早丢失任务？

**参考回答：** Publisher Confirm覆盖生产者到 Broker 的接收；`mandatory` 配合 return 用来发现消息到达 Exchange 但没有路由到队列；消费者 ACK 表示这条投递已经完成处理。AiVista 的 Worker 不会在刚收到消息时 ACK，而是在可重放结果已写入 Ledger、Java Completion 已确认后再 ACK。这样进程中途退出，Broker 会重新投递。三种机制覆盖不同边界，任何一种都不能替代另外两种。

**来源：** S4、S5

### Q040｜P0｜简历直接追问｜Agent Worker / MySQL

**主问题：** RabbitMQ 已经提供 ACK，Worker 为什么还要持久化执行 Ledger？

**连续追问：** ① ACK 能否记录外部模型调用已经进行到哪一步？ ② Worker 重启后靠什么决定继续、复用还是停止？

**参考回答：** ACK 只管理 RabbitMQ 这次投递，不知道百炼是否已调用、图片是否已上传，也无法在 Worker 重启后恢复中间阶段。Ledger 把执行权和关键检查点持久化到 MySQL：READY 表示可执行，CALLING 表示外部调用阶段，STAGED 表示结果已落到可复用位置，COMPLETED 表示 Java 已确认。重投后 Worker 根据状态决定开始、复用已暂存结果或直接确认完成，而不是每次都重新调用模型。

**来源：** S2、S4

### Q041｜P0｜简历直接追问｜Agent Worker / 架构

**主问题：** READY、CALLING、STAGED、COMPLETED 这些执行状态分别对应哪些崩溃窗口？

**连续追问：** ① 为什么业务任务状态和 Worker 执行状态不能混成一套？ ② 哪个服务拥有各自状态的写权限？

**参考回答：** 这两套状态回答的问题不同。Java 的任务状态面向用户和业务事务，例如 QUEUED、SUCCEEDED、FAILED、CANCELLED；Worker Ledger 描述一次外部执行走到哪一步。READY 到 CALLING 用来取得执行权，STAGED 表示副作用结果已可复用，COMPLETED 表示业务提交得到确认。Java 只写业务表，Worker 只写 `generation_worker_executions`，DDL 仍由 Java Flyway 管理。拆开后不会把临时执行细节暴露成用户状态，也避免两个服务同时修改同一事实。

**来源：** S2

### Q042｜P0｜面经原型｜Agent Worker / Java 服务端

**主问题：** Worker 调用模型成功并上传 OSS 后，在提交 Java Completion 前崩溃，消息重投后怎么办？

**连续追问：** ① 确定性对象 Key 能解决什么？ ② 如果本地 Ledger 也没有成功写入，系统还能否判断外部调用结果？

**参考回答：** 如果图片已经下载并用确定性 Key 上传，重投时可以定位和复用同一对象，而不是不断生成新路径。Worker 应先把可重放结果写成 STAGED，再调用 Java Completion；这样 Completion 前崩溃可以从 Ledger 恢复。如果模型成功但 Worker 在任何结果持久化前就崩溃，系统通常无法确定 Provider 是否完成，这就是 outcome unknown。没有 Provider 的幂等请求或结果查询接口时，我不会自动重试，以免重复计费和生成。

**来源：** S2、S4

### Q043｜P1｜简历直接追问｜Agent Worker / 架构

**主问题：** 为什么“Provider 调用结果未知”的场景不能直接自动重试？

**连续追问：** ① 请求明确未发出和响应中途断开有什么区别？ ② 如何避免一次用户请求产生两组计费图片？

**参考回答：** 连接建立前就失败，可以确认请求没有到达 Provider，通常允许有限重试；请求发出后超时或连接中断，只能说没有收到结果，不能说明 Provider 没执行。后者如果自动重试，可能产生第二组计费图片。AiVista 对这种不确定结果采用保守策略，不继续盲重试，并把失败分类持久化。更理想的方案是 Provider 支持客户端幂等键或按请求 ID 查询结果，再由 Worker恢复，而不是自行猜测。

**来源：** S2

### Q044｜P1｜面经原型｜Agent Worker / Java 服务端

**主问题：** 结果提交接口为什么必须幂等，幂等键应该绑定哪个业务层级？

**连续追问：** ① 只使用任务 ID 有什么风险？ ② 相同幂等键携带不同请求内容时应该怎么识别？

**参考回答：** Completion 可能因为网络超时被重复提交，所以 Java 要用稳定的 `completionId` 识别同一次完成，并保存请求指纹与已返回结果。只看任务 ID 会混淆正常重放和同一任务上内容不同的错误提交。相同 ID、相同指纹时返回第一次结果；相同 ID 但指纹不同应拒绝并记录冲突，不能悄悄覆盖。Agent Tool 创建子生成任务时，幂等键由 Creation Task ID 和 `toolCallId` 稳定派生。

**来源：** S4、S5

### Q045｜P1｜面经原型｜Java 服务端

**主问题：** 用户重复点击创建、浏览器自动重试和网关重试同时发生时，服务端如何防止重复任务？

**连续追问：** ① 幂等记录应该与业务写入处于同一事务吗？ ② 并发请求第一次都查不到记录时怎么处理？

**参考回答：** 前端每次创建携带 UUID 格式的 `Idempotency-Key`，Java 同时计算规范化请求指纹。幂等记录、任务、额度和 Outbox 在同一事务中写入；重复请求如果 Key 和指纹一致，直接返回第一次的任务与版本，指纹不同则拒绝。并发首次请求还要靠唯一约束和加锁读取处理竞争，不能只做一次“先查后插”。浏览器、网关即使同时重试，最后也只保留一个业务任务。

**来源：** S5

### Q046｜P1｜简历直接追问｜Java 服务端 / 前端

**主问题：** Java 服务如何阻止旧 Worker 结果覆盖已经取消或更新后的任务状态？

**连续追问：** ① `taskVersion` 在写入条件和前端对账中分别怎么使用？ ② 只判断当前状态而没有版本号是否足够？

**参考回答：** 每次有效状态变化都会推进 `taskVersion`。Java 接受 Worker Completion 时，不只检查任务 ID，还用预期状态和版本做条件更新；用户取消后版本已变化，旧 Worker 的成功结果就无法覆盖。前端收到 SSE 时也只接收不旧于当前快照的版本，断线后再以 REST 返回的版本对账。只判断状态不够，因为相同状态可能来自不同时刻，版本号能明确事件的新旧关系。

**来源：** S2

### Q047｜P1｜八股延伸｜MySQL / Java 服务端

**主问题：** 条件更新和幂等记录依赖 MySQL 的哪些事务隔离、锁和 MVCC 行为？

**连续追问：** ① 可重复读和读已提交下的表现可能有什么不同？ ② 唯一索引在并发幂等创建中承担什么作用？

**参考回答：** MVCC 让普通一致性读看到事务快照，条件更新和 `SELECT ... FOR UPDATE` 则会参与当前读与加锁。可重复读通常保持同一事务内的读视图，并通过 next-key lock 处理部分范围并发；读已提交每条语句获取较新的视图，锁范围也可能更小。AiVista 的正确性不依赖“碰巧读不到另一个事务”，而是依赖唯一索引、行锁和带状态版本的条件更新。唯一索引是并发首次创建的最终防线，冲突后再读取已有幂等结果。

**来源：** S5

### Q048｜P1｜面经原型｜架构

**主问题：** 这套系统能不能宣称端到端 Exactly Once？

**连续追问：** ① MQ 的投递语义和业务处理效果是否是一回事？ ② OSS 上传和模型计费这样的外部副作用如何讨论“一次性”？

**参考回答：** 我不会宣称端到端 Exactly Once。RabbitMQ 和 Outbox提供的是至少一次投递，业务层通过幂等键、Ledger、确定性对象 Key 和条件状态转换实现“最终只确认一次”的效果。数据库内的终态和资产可以原子收敛，但模型计费、HTTP 调用和 OSS 这类外部副作用不在同一事务里。尤其 outcome unknown 无法凭本地状态证明只发生一次，所以准确说法是至少一次消息加业务幂等，而不是全链路恰好一次。

**来源：** S4、S5

### Q049｜P1｜八股延伸｜Java 服务端 / MySQL

**主问题：** 为什么不能开启一个数据库事务，然后在事务里等待模型生成几分钟？

**连续追问：** ① 长事务会对连接池、锁和 MVCC 版本清理造成什么影响？ ② 事务边界应该如何围绕本地状态变化划分？

**参考回答：** 模型调用可能持续几分钟，把它放进事务会长期占用数据库连接，延长锁持有时间，让并发请求阻塞，还会保留旧的 MVCC 版本，增加回滚和清理成本。项目把事务限制在本地、短小、可原子提交的状态变化上：创建任务与 Outbox 一次事务，Worker执行在事务外，Java Completion 再用一次事务确认终态、额度和资产。远程调用通过状态机和幂等衔接，而不是试图跨网络维持一个大事务。

**来源：** S5

### Q050｜P1｜面经原型｜Agent Worker / RabbitMQ / 优化

**主问题：** 消费并发、RabbitMQ prefetch 和模型供应商限流之间应该如何配合？

**连续追问：** ① prefetch 太大或太小分别可能发生什么？ ② 普通 Generation Consumer 和 Agent Consumer 为什么使用独立并发池？

**参考回答：** prefetch 决定一个消费者尚未 ACK 的消息数量。太大会让长任务集中压在少数 Worker 上，增加内存占用和重启后的重投量；太小会降低吞吐，但能提供更公平的分发。当前 Agent Consumer 使用 `prefetch(1)`，因为一次任务内部还可能等待多个生成任务，生命周期长。普通生成与 Agent 使用独立队列和并发池，避免 Agent 占满许可后拖住普通生图。最终并发还要受 Provider 配额和本地资源限制，不能只增加消费者数量。

**来源：** S2、S4

### Q051｜P1｜八股延伸｜RabbitMQ / 架构

**主问题：** Quorum Queue、普通经典队列和持久化消息分别解决什么问题？

**连续追问：** ① 队列具备副本是否意味着消息绝不会丢？ ② Publisher Confirm 与队列副本提交之间是什么关系？

**参考回答：** Quorum Queue 用多数派复制提高节点故障下的数据安全和可用性，经典队列的复制与故障语义不同；消息持久化只是让数据具备落盘条件。它们都不代表绝对不丢，错误配置、多数节点丢失、过期策略或运维操作仍可能造成数据不可用。Publisher Confirm 表示 Broker 按所选队列语义接收了发布，生产者才能推进 Outbox，但它仍不能替代 mandatory 路由检查和消费端 ACK。

**来源：** S4

### Q052｜P1｜面经原型｜Java 服务端 / 架构

**主问题：** 任务一直停留在 QUEUED，没有消费者处理，系统怎样判断它是正常排队还是已经失联？

**连续追问：** ① 排队超时为什么由 Java Core 负责？ ② 超时扫描与迟到的 Worker Completion 发生竞态时怎么办？

**参考回答：** QUEUED 是 Java 的业务状态，超时会影响额度、用户提示和是否接受后续结果，因此由 Java 定时扫描并更新，不能让 Worker随意判定。扫描时使用状态、版本和更新时间做条件更新，只能终止仍满足旧条件的任务。迟到 Completion 同样带预期信息进入事务，谁先成功推进版本，另一方就因条件不匹配被拒绝。这样超时和完成不会互相覆盖，最终状态有明确的获胜顺序。

**来源：** S2、S4

### Q053｜P2｜面经原型｜Java 服务端 / RabbitMQ

**主问题：** Outbox Dispatcher 多实例同时扫描同一批事件时，如何避免重复领取和相互覆盖？

**连续追问：** ① 悲观锁、条件更新和租约各有什么取舍？ ② 领取后实例崩溃，PROCESSING 事件如何重新可用？

**参考回答：** 多实例 Dispatcher 可以先扫描候选，再用 `status=PENDING` 的条件更新抢占，只有更新成功的实例拥有发布权；也可以用数据库跳锁查询减少竞争。长时间持有悲观锁不合适，因为发布 MQ 是远程 I/O。领取状态要记录时间或租约，实例崩溃后，另一扫描器把超过租约的 PROCESSING 事件重新调度。即使极端情况下重复发布，消费端幂等仍是最后一道保证。

**来源：** S2、S5

### Q054｜P2｜八股延伸｜Java 服务端 / Spring

**主问题：** Spring 的 `@Transactional` 通过什么机制生效，类内自调用为什么可能让事务失效？

**连续追问：** ① 异常被捕获后不再抛出会怎样影响回滚？ ② 异步线程中能否自动继承原事务？

**参考回答：** Spring 默认通过代理拦截外部方法调用，在进入方法前开启事务，正常返回时提交，满足回滚规则的异常抛出时回滚。类内 `this.method()` 没经过代理，目标方法上的 `@Transactional` 可能不生效；异常如果被吞掉，代理看到正常返回也会提交，除非显式标记回滚。事务上下文通常绑定当前线程，异步线程不会自动继承，所以 Outbox Dispatcher 和 Worker要有自己的事务边界。

**来源：** S5

### Q055｜P2｜简历直接追问｜Agent Worker / Java 服务端

**主问题：** Java Completion 已提交成功，但 Worker 没收到响应并再次提交，系统如何返回同一个结果？

**连续追问：** ① Completion ID 和请求指纹分别解决什么问题？ ② 第二次提交的资源列表不同应该怎么处理？

**参考回答：** Worker 没收到响应时会再次用同一个 Completion ID 提交。Java 第一次成功后保存该 ID、请求指纹和响应；重放时如果指纹一致，直接返回已确认结果，不再重复创建资产或调整额度。Completion ID 用来定位同一次操作，指纹用来防止同一个 ID 被错误复用。第二次提交的资源列表不同意味着协议冲突，必须拒绝并告警，不能把它当作普通重试。

**来源：** S2、S4

### Q056｜P2｜面经原型｜架构 / 优化

**主问题：** 如果任务量增长十倍，Outbox 表、Dispatcher、RabbitMQ 和 Worker 哪一处最先成为瓶颈？

**连续追问：** ① 你会怎样用指标而不是直觉确定瓶颈？ ② 分区、批量发布和增加消费者分别有哪些前提？

**参考回答：** 我会先看 Outbox 最老事件年龄和扫描耗时、RabbitMQ ready/unacked 数量、消费者利用率、Provider 延迟、数据库慢查询与连接池，而不是先猜某个组件。若 Outbox 查询和更新成为热点，可以加合适索引、批量领取或按时间归档；Broker 积压但 Worker 空闲要检查路由和 prefetch；Worker 满载且 Provider 有余量才增加消费者。表分区和批量发布会增加实现复杂度，只有数据量和指标证明单表或逐条发布成为瓶颈时再做。

**来源：** S2、S4

---

## 6. 实时交互（18 题）

### Q057｜P0｜简历直接追问｜Agent Worker / Java 服务端 / 前端

**主问题：** 模型回复、创作说明和 Tool 状态从 Pi Agent 到浏览器要经过哪些组件？

**连续追问：** ① 哪些事件只用于实时展示，哪些还会形成持久化快照？ ② 为什么 Worker 不直接向浏览器提供连接？

**参考回答：** Pi 事件先由 Worker 的 Normalizer 转成 AiVista 的文本、Tool、Skill 和运行状态事件，并做必要的合并。它们通过 Worker 到 Java 的内部 WebSocket 发送，Java 再投影成浏览器 SSE。逐段文本主要改善实时体验，不逐 Token 落库；Tool、Skill 稳定边界会提前幂等持久化，最终 Completion 再全量兜底。浏览器只连接 Java，因为 Java掌握认证、用户权限和业务快照，也避免 Worker 暴露公网 API 或与前端形成第二套身份体系。

**来源：** S3

### Q058｜P0｜面经原型｜架构 / 前端 / Java 服务端

**主问题：** 为什么 Worker 到 Java 使用 WebSocket，而 Java 到浏览器使用 SSE？

**连续追问：** ① 两段链路的通信方向有什么不同？ ② 如果全部统一成 WebSocket，会增加哪些成本？

**参考回答：** Worker 与 Java 之间既要上报实时事件，也要由 Java 下发取消和心跳控制，所以使用双向 WebSocket。浏览器侧的主要模式是先用 HTTP 提交任务，再持续接收服务端事件，SSE 更贴合单向推送，也能复用现有 HTTP 鉴权和网关。全部改成 WebSocket 当然能做，但要额外处理浏览器连接鉴权、命令协议、心跳、连接恢复和多实例路由。当前取消用普通 HTTP 提交即可，没有必要为了少量上行控制把浏览器链路都改成 WebSocket。

**来源：** S7、S8

### Q059｜P0｜八股延伸｜前端 / 网络

**主问题：** SSE、WebSocket、长轮询和普通 HTTP 轮询的连接方式与适用场景分别是什么？

**连续追问：** ① SSE 在 HTTP/1.1 和 HTTP/2 下有哪些连接层差异？ ② WebSocket 握手与普通 HTTP 请求是什么关系？

**参考回答：** 普通轮询每次请求拿一个快照，长轮询让服务端暂时挂起请求；SSE 保持 HTTP 响应并按事件持续单向推送；WebSocket 从 HTTP 握手升级为全双工连接。SSE 适合模型输出和任务状态，WebSocket 适合双方都频繁发消息的协作或控制。HTTP/1.1 下浏览器对同源并发连接数有限，HTTP/2 可以在一条连接上多路复用流。WebSocket 的初始握手仍是 HTTP 请求，成功升级后才使用 WebSocket 帧通信。

**来源：** S6、S7、S8

### Q060｜P0｜简历直接追问｜前端

**主问题：** 前端为什么使用 `fetch` 读取 SSE 流，而不是直接使用原生 `EventSource`？

**连续追问：** ① Authorization 请求头会怎样影响选择？ ② 自己解析事件流时如何处理半包、多行 data 和 UTF-8 边界？

**参考回答：** 原生 `EventSource` 使用方便，但不能灵活设置 Authorization 请求头，而 AiVista 的 SSE 需要沿用 Bearer Token，所以前端用 `fetch` 获取响应流。解析时不能假设一次 chunk 就是一条事件：先用流式 `TextDecoder` 保留跨 chunk 的 UTF-8 状态，再把文本追加到缓冲区，按空行切分完整事件，合并多行 `data:`，最后根据 `event` 和 JSON 内容分发。连接关闭时还要处理残余缓冲和取消信号。

**来源：** S3、C2

### Q061｜P0｜面经原型｜前端 / 优化

**主问题：** 模型文本高频到达导致 React 组件不断重渲染时，你会怎么定位和优化？

**连续追问：** ① 更新频率、组件范围和 Markdown 解析成本分别怎么测？ ② 批量刷新会怎样影响用户感知延迟？

**参考回答：** 我会先用 React Profiler 和浏览器 Performance 看事件频率、提交次数、耗时组件和 Markdown 解析占比。优化时把正在流式变化的消息与稳定历史消息拆开，按动画帧或几十毫秒批量合并 delta，用 ref 暂存缓冲，只更新受影响的消息，并缓存稳定内容的渲染结果。批量间隔太大会让文字明显卡顿，太小又失去意义，所以需要用长回复实测输入延迟和每秒提交次数，而不是机械地加 `useMemo`。

**来源：** S6、C2

### Q062｜P0｜简历直接追问｜Agent Worker / 前端 / 安全

**主问题：** 哪些 Pi 事件允许同步到前端，哪些事件不应该暴露给用户？

**连续追问：** ① 为什么不保存或转发模型隐藏 Thinking？ ② Tool 参数中如果含有内部地址或敏感信息怎么处理？

**参考回答：** 前端只需要用户可理解的文本、创作说明、Tool 名称和进度，以及稳定的开始、结束状态。隐藏 Thinking 不属于产品输出，可能包含不稳定推理、敏感上下文，也没有必要持久化。事件适配层会把 SDK 事件投影成自己的白名单协议，而不是透传原对象；Tool 参数和结果只保留可公开字段，内部 URL、Token、对象 Key 和错误堆栈都不下发。最终展示以 Java 的 DTO 和权限规则为准。

**来源：** S3

### Q063｜P0｜面经原型｜前端 / Java 服务端 / 网络

**主问题：** SSE 在代理缓冲、空闲超时、网络切换和浏览器连接数限制下可能出现哪些问题？

**连续追问：** ① 心跳事件解决的是哪类问题？ ② 为什么服务端已经发送数据，浏览器仍可能长时间看不到？

**参考回答：** SSE 可能被反向代理缓冲，直到积累一定数据才刷给浏览器；代理或负载均衡也可能把长时间无数据的连接当作空闲连接关闭。服务端需要设置正确的流式响应头、及时 flush，并在空闲期间发送轻量心跳。心跳能保持链路活跃并帮助客户端发现断线，但不能恢复业务状态。网络切换后客户端重新连接，仍要拉 REST 快照。HTTP/1.1 的同源连接上限也要考虑，避免一个页面开启多条不必要的 SSE。

**来源：** S6、S7、C2

### Q064｜P1｜简历直接追问｜前端 / Java 服务端

**主问题：** SSE 断线重连后，前端如何通过 REST 快照恢复任务进度？

**连续追问：** ① 重连时应该先订阅还是先拉取快照？ ② 如何避免在两个请求之间漏掉终态？

**参考回答：** 核心原则不是依赖某个瞬时顺序，而是所有状态都带可比较的版本。客户端可以先建立 SSE 再拉快照，缓存期间到达的事件，拿到快照后按 `taskVersion` 合并；也可以拉快照后立刻订阅，再对目标任务补一次查询。AiVista 把 SSE 当通知和实时体验，REST 快照才是事实来源。只要旧版本不覆盖新版本，并在重连后主动对账，即使中间漏掉一条事件也能恢复终态。

**来源：** S7

### Q065｜P1｜八股延伸｜前端 / Java 服务端

**主问题：** REST 快照和延迟到达的旧 SSE 事件发生冲突时，前端如何决定采用哪一个状态？

**连续追问：** ① `taskVersion` 应该按任务还是按连接维护？ ② 相同版本的重复事件是否还需要处理？

**参考回答：** 版本属于任务，不属于 SSE 连接。前端为每个任务保存当前最大 `taskVersion`：收到更小版本直接丢弃，更大版本更新；相同版本的重复状态事件按幂等处理，不重复弹提示或追加结果。REST 快照如果版本更高就覆盖本地投影，旧 SSE 即使迟到也不会回滚页面。对于不属于任务状态的文本 delta，则使用 Creation 和消息标识单独聚合，不能把所有事件都混用同一个版本规则。

**来源：** S7

### Q066｜P1｜面经原型｜前端

**主问题：** 用户刷新页面后，为什么不能只依赖 Zustand 或 localStorage 恢复正在执行的任务？

**连续追问：** ① 浏览器缓存被清除或多设备登录时会怎样？ ② 哪些 UI 状态适合留在本地，哪些必须由服务端恢复？

**参考回答：** Zustand 和 localStorage 只适合保存当前面板、草稿或临时筛选这类界面状态，不能作为正在执行任务的事实来源。刷新、清缓存、换设备都会丢本地数据，而且本地记录可能已经过期。任务、消息、Activity、资产和版本都由 Java 持久化；页面加载后根据会话和最新任务接口恢复，再重新订阅事件。这样刷新不会重置任务，也不会因为本地缓存伪造一个仍在运行的状态。

**来源：** S7

### Q067｜P1｜简历直接追问｜前端 / Java 服务端 / Agent Worker

**主问题：** 用户点击取消后，请求如何贯穿 Java 权威事务、内部 WebSocket、Pi Session 和 Tool？

**连续追问：** ① 如果 Worker 的实时连接此时断开，取消状态如何最终收敛？ ② 哪一层负责拒绝迟到的成功结果？

**参考回答：** 取消首先是 Java 的权威业务操作：在事务中把 Creation 和相关任务转为取消并推进版本，然后尽力通过内部 WebSocket 通知当前 Worker。Worker在线时会中止 Pi 并触发 Tool 的 AbortSignal；连接断开时实时中止可能迟到，但 Java 状态已经生效。Worker 后续重连会读取快照对账，迟到的 Completion 也会因状态或版本不匹配被 Java 拒绝。因此 WebSocket 负责加快停止，最终正确性不依赖它一定送达。

**来源：** S2、S3

### Q068｜P1｜面经原型｜Agent Worker / 架构

**主问题：** 图片 Provider 已经开始生成时，用户取消能否保证图片不会产生？

**连续追问：** ① “停止等待”“停止扣费”和“停止外部执行”是不是同一件事？ ② 已生成但不再写入 Agent 最终回复的资产怎样处置？

**参考回答：** 这三件事不同。AbortSignal 可以让本地 Tool 停止等待，Java 可以阻止 Agent Creation 继续写回，但如果 Provider 不支持取消，已经发出的生图请求可能仍会执行并计费。普通 Generation Pipeline 会把已经明确完成的结果可靠收敛成用户资产，避免形成无法追踪的 OSS 孤儿；已取消 Agent 不再引用它，也不会继续生成最终说明。如果业务希望取消后连资产都不保留，还需要额外的清理策略，不能假设外部调用被物理撤销。

**来源：** S2

### Q069｜P1｜八股延伸｜前端 / Java 服务端

**主问题：** SSE 客户端重连应该使用固定间隔、指数退避还是服务端指定的 retry？

**连续追问：** ① 为什么需要加入抖动？ ② 页面从离线恢复时怎样避免所有客户端同时重连？

**参考回答：** 固定间隔会让大量客户端在服务恢复时同时发起请求，形成新的尖峰。指数退避让连续失败后的等待逐步增长，再加入随机抖动打散客户端；如果服务端 SSE 提供 `retry`，客户端可以把它作为基线，但仍要设置上限。浏览器从离线变在线时，不应立即无限并发重连，可以先确认网络状态、随机延迟，再建立单一连接并拉快照。手动取消或页面卸载则不应触发自动重连。

**来源：** S7、C2

### Q070｜P1｜简历直接追问｜Agent Worker / Java 服务端

**主问题：** Tool 和 Skill 的稳定边界为什么要提前持久化 Activity，最终 Completion 为什么还要全量兜底？

**连续追问：** ① 只保存逐 Token 事件会有什么问题？ ② 提前投影和最终投影怎样做到幂等？

**参考回答：** 逐 Token 事件数量大、价值低，断线时还可能缺段，不适合作为业务事实。Tool 开始、Tool 完成、Skill 披露这类稳定边界对用户有意义，所以会提前写入 Activity；最终 Completion 携带完整结果，再补齐遗漏。两条路径使用稳定的 Activity 标识和唯一约束，重复写入时返回已有记录。这样在线用户能及时看到进度，断线用户又能靠最终快照恢复，不需要重放所有 Token。

**来源：** S2

### Q071｜P2｜面经原型｜前端

**主问题：** SSE 流式 Markdown 在标记尚未闭合时，前端如何避免内容闪烁或结构反复变化？

**连续追问：** ① 每个字符都重新解析完整 Markdown 的复杂度怎样？ ② 代码块和图片语法处于半完成状态时如何展示？

**参考回答：** 如果每个 delta 都对整段文本重新做 Markdown 解析，总工作量会随着文本增长接近平方级，还会让未闭合的加粗、代码块反复改变 DOM。可以把流式中的内容按较低频率批量刷新，稳定段落做缓存，最后结束时再完整解析。未闭合代码块可以暂时按纯文本或宽松模式展示，图片语法在 URL 和闭合符完整前不要发起请求。这里要优先保证可读和稳定，没必要追求每个字符都立即富文本化。

**来源：** C2

### Q072｜P2｜八股延伸｜网络 / 前端

**主问题：** TCP 本身已经是字节流，为什么应用层还需要 SSE 的事件边界？

**连续追问：** ① 一次 `read()` 是否一定对应服务端的一次 `send()`？ ② 客户端怎样识别一条事件结束？

**参考回答：** TCP 只提供有序字节流，不保留应用层消息边界。服务端一次写入可能被客户端拆成多次读取，多次写入也可能合并到一个 chunk，因此不能按 `read()` 次数解析消息。SSE 在字节流上定义了文本事件格式：字段按行组织，空行表示一个事件结束，多行 `data:` 需要合并。客户端还要用流式解码处理跨 chunk 的 UTF-8 字符，等完整事件出现后再解析 JSON。

**来源：** S8、C2

### Q073｜P2｜面经原型｜架构 / Java 服务端

**主问题：** Java Core 扩展到多个实例后，SSE 连接和任务状态事件如何路由到正确实例？

**连续追问：** ① 只依赖当前实例内存中的连接表有什么限制？ ② Redis Pub/Sub、Broker 广播和连接粘滞分别如何取舍？

**参考回答：** 当前 Java SSE 连接表和状态事件分发按单实例设计，多实例后某个任务事件可能到达没有对应浏览器连接的实例。可以让网关做连接粘滞，但后台事件仍要路由；更通用的是通过 Redis Pub/Sub 或消息 Broker 把事件广播或按用户分区，再由持有连接的实例发送。Pub/Sub 简单但离线不保留，Broker 更可靠也更重。无论采用哪种方式，REST 快照仍是恢复依据，不能把正确性寄托在实时广播上。

**来源：** S2、S7

### Q074｜P2｜面经原型｜架构 / Agent Worker

**主问题：** Worker 扩展到多个实例后，取消控制帧怎样找到真正执行该 Agent Session 的实例？

**连续追问：** ① 实例级连接映射丢失时怎么办？ ② 分布式租约、共享取消信号和按任务路由分别有什么代价？

**参考回答：** 当前 Worker 的 Pi Session 和 Tool 等待器都在进程内，所以 Java 可以通过实例级 WebSocket 定向取消。多实例后需要在任务开始时登记执行实例和租约，取消命令按任务路由到该实例；也可以把取消标志放进共享存储，让执行端周期检查。租约能处理实例失联，但要解决续约和时钟；共享轮询简单但不够实时；Broker 路由实时但要维护实例拓扑。最终仍以 Java 的取消状态和版本拒绝迟到结果。

**来源：** S2、S7

---

## 7. 资产闭环（16 题）

### Q075｜P0｜简历直接追问｜Java 服务端 / Agent Worker

**主问题：** Agent Tool 生成的结果在什么时机转化为正式用户资产？

**连续追问：** ① OSS 对象已经存在但 Java Completion 失败时处于什么状态？ ② 资产、任务终态和额度为什么要在 Java 事务中统一确认？

**参考回答：** Worker 完成模型调用和 OSS 转存后，并不会直接把结果当成用户资产，而是先把可重放结果写入 Ledger，再调用 Java 的 Completion。Java 在一个事务里校验任务状态和版本，创建图片资产、更新任务终态、处理额度并写状态事件。只有事务提交后，这些图片才成为业务上可见的正式资产。OSS 对象先存在但 Completion 失败时只是暂存结果，Worker 可以重试提交；长期无法归属的对象由清理流程按确定性 Key 和业务记录处理。

**来源：** S2

### Q076｜P0｜简历直接追问｜Java 服务端 / 前端 / 优化

**主问题：** 为什么为一张生成图片保存原图、卡片图和展示图三个对象？

**连续追问：** ① 640 和 1600 这类尺寸应该根据什么确定？ ② 只保存原图并依赖浏览器缩放会有什么问题？

**参考回答：** 三种对象对应不同使用场景：原图用于作者下载，640 宽的卡片图用于瀑布流和列表，1600 宽的展示图用于详情与公开展示。尺寸要根据实际容器宽度、DPR、视觉质量和带宽权衡，不是越大越好。只发原图会浪费流量、增加解码内存和首屏等待，浏览器缩放也不会减少下载体积。预生成派生图增加了一些存储和处理成本，但能换来稳定、可控的交付性能。

**来源：** S6

### Q077｜P0｜面经原型｜前端 / 优化

**主问题：** 弱网环境下用户快速滚动图片列表，怎样避免白屏、重复下载和页面抖动？

**连续追问：** ① 懒加载、预加载和请求取消各在什么时机使用？ ② 图片尺寸未知为什么会引发布局偏移？

**参考回答：** 列表只为视口附近图片设置真实地址，临近视口时预加载下一批；用户快速滚动离开后，可以取消还没必要完成的请求。接口返回图片宽高或固定宽高比，页面先预留空间，避免图片加载完成后把下方内容推开。卡片先用缩略图，需要时再切展示图，并利用浏览器缓存避免重复下载。还要限制并发、提供失败占位和重试入口，不能让几十张大图同时争用弱网带宽。

**来源：** S6

### Q078｜P0｜简历直接追问｜Java 服务端 / 安全

**主问题：** 普通资产列表和详情接口为什么不能直接返回原图 URL？

**连续追问：** ① 作者下载与公开展示需要怎样区分权限？ ② 数据库中为什么不持久化短期签名 URL？

**参考回答：** 普通列表和详情 DTO 只返回缩略图或展示图的短期签名地址，不返回原图。原图下载走独立接口，Java 先确认当前用户是作者，再为原图对象签名。公开展示只代表允许访问展示变体，不代表任何人都能取得原始文件。签名 URL 带过期时间，持久化后很快失效，也可能被错误复用，所以数据库只保存稳定对象 Key，请求时按用途和权限临时签发。

**来源：** S6

### Q079｜P0｜八股延伸｜Java 服务端 / 安全

**主问题：** 未发布私有图片、公开展示图和作者原图下载分别应该在哪一层鉴权？

**连续追问：** ① 资源不存在和无权访问为什么常统一返回 404？ ② 只依赖 OSS 对象路径不可猜测是否安全？

**参考回答：** 私有资产查询先在 Java 校验登录用户、资产归属和发布状态，再决定可以签发哪种变体。未发布图片只有作者可看；公开作品只暴露展示图；原图下载仍要求作者身份。对不存在和无权访问统一返回 404，可以减少攻击者枚举资产 ID 的信息差。对象路径随机或难猜只能降低碰撞概率，不能替代鉴权；OSS Bucket 保持私有，浏览器拿到的是短期签名 URL。

**来源：** S5、S6

### Q080｜P0｜面经原型｜Java 服务端 / 架构

**主问题：** 用户删除资产时，数据库记录、OSS 三种对象、发布记录和搜索索引如何收敛？

**连续追问：** ① 为什么不在用户请求事务中同步等待 OSS 删除？ ② 重复删除对象和删除失败分别怎么处理？

**参考回答：** 删除时先由 Java 事务处理业务可见性，让资产、发布状态和后续搜索投影先失效，再异步清理 OSS 的原图、卡片图和展示图。数据库事务无法和 OSS 组成同一个本地事务，如果在请求里等待远程删除，会拉长事务并让临时网络故障阻塞用户操作。OSS 删除按幂等设计，对象已经不存在也视为成功；失败则记录并重试。搜索索引通过 Outbox 最终更新，查询候选仍会回到 MySQL 做状态校验。

**来源：** S2、C3

### Q081｜P1｜八股延伸｜前端 / Java 服务端 / CDN

**主问题：** OSS 签名 URL 到期后，浏览器缓存和未来 CDN 缓存可能分别出现什么行为？

**连续追问：** ① URL 查询参数变化会怎样影响缓存命中？ ② 私有源站与公开缓存之间如何避免绕过权限？

**参考回答：** 直接使用 OSS 签名 URL 时，过期后需要重新向 Java 获取新地址；查询参数变化可能让浏览器和 CDN 视为不同缓存键，降低命中率。未来接 CDN 私有源站时，需要明确缓存键是否包含签名、边缘鉴权方式、对象公开范围和 TTL，不能让公开缓存长期保存作者原图。展示变体可以采用较稳定的授权策略，原图下载则保持短时、用户级签名。当前 CDN 链路尚未完成，所以这些属于设计边界，不是已上线能力。

**来源：** S6

### Q082｜P1｜简历直接追问｜架构

**主问题：** 当前 CDN 私有源站链路是否已经完成，简历和面试中应该如何描述它的状态？

**连续追问：** ① 已实现的 OSS 私有对象与待实现的 CDN 分发边界在哪里？ ② 如何避免把设计方案误说成线上成果？

**参考回答：** 已实现的是私有 OSS 对象、三种图片变体、Java 权限校验和短期签名 URL。README 中的“图片 CDN 链路”仍在持续完善，私有源站、边缘缓存策略和生产域名没有完成。我在面试中会直接说“当前用 OSS 签名 URL，下一步计划接私有 CDN”，然后说明为何需要它以及要解决的鉴权和缓存问题。这样既能讨论方案，也不会把文档里的优化设计冒充为实测成果。

**来源：** C3

### Q083｜P1｜面经原型｜Java 服务端 / 搜索

**主问题：** 作品审核通过、撤回发布或删除后，Meilisearch 索引如何保持最终一致？

**连续追问：** ① 为什么索引更新事件只携带 imageId？ ② 消费索引事件时为什么要重新读取 MySQL？

**参考回答：** 发布审核、撤回和删除都在 MySQL 事务中修改权威状态，并写搜索 Outbox。事件只带 imageId，索引处理器消费时重新查询当前发布状态和字段，再决定 upsert 还是 delete。这样多个事件乱序或延迟时，处理器读取的是最新事实，不会把过期的标题、作者或发布状态写回索引。失败事件可以重试，搜索短暂不一致时，查询结果还会由 MySQL 二次过滤。

**来源：** S2、C3

### Q084｜P1｜八股延伸｜搜索 / Java 服务端

**主问题：** Meilisearch 返回已经下架、删除或权限变化的图片时，服务端为什么还要二次校验？

**连续追问：** ① 搜索索引能否作为业务事实来源？ ② 候选结果被过滤后当前页数量不足怎么办？

**参考回答：** Meilisearch 只是检索投影，更新是异步的，所以不能决定作品是否仍公开。搜索先返回候选 imageId，Java 再按 MySQL 当前状态、删除标记和权限查询真正可见的作品，并保持搜索顺序。如果部分候选失效，当前页可能不足；可以在限制范围内继续拉取更多候选补齐，但要设置扫描上限，避免索引严重陈旧时无限循环。索引最终由 Outbox 修复，MySQL 始终是事实来源。

**来源：** C3

### Q085｜P1｜面经原型｜搜索 / 架构

**主问题：** 为什么选择 Meilisearch，而不是 MySQL `LIKE`、全文索引或 Elasticsearch？

**连续追问：** ① 数据规模、中文搜索、运维成本和功能需求分别如何影响选型？ ② 如果未来需要复杂聚合与分析，当前选型是否还合适？

**参考回答：** MySQL `LIKE` 实现简单，但模糊匹配和相关性排序有限，数据增长后也难以提供良好的中文搜索体验。Elasticsearch 能力最强，但集群、Mapping 和运维成本对个人项目偏重。Meilisearch 接入轻、搜索体验和索引管理满足当前作品关键词发现，所以我选择它作为投影。如果以后需要复杂聚合、日志分析、细粒度分词和大规模分片，我会重新评估 Elasticsearch，而不是因为当前选型就排斥迁移。

**来源：** S4、C3

### Q086｜P1｜简历直接追问｜搜索 / Java 服务端

**主问题：** 全量重建搜索索引时，为什么使用新索引加原子 swap，而不是清空旧索引后重建？

**连续追问：** ① 重建期间增量事件怎么处理？ ② swap 前怎样验证新索引完整可用？

**参考回答：** 直接清空线上索引会让搜索在重建期间不可用，失败时也没有可回退版本。项目可以把 MySQL 全量数据写入临时索引，配置相同的可搜索、过滤和排序字段，核对任务完成状态、文档数量和抽样结果后，再用原子 swap 切换。重建期间的增量事件仍要记录，可以在全量基线后重放，或在 swap 前后做一次追平。旧索引保留到验证完成，出现问题可以快速换回。

**来源：** S2

### Q087｜P1｜面经原型｜Java 服务端 / 优化

**主问题：** 点赞当前直接写 MySQL，什么迹象出现后才值得改成 Redis Lua 加 RabbitMQ？

**连续追问：** ① 你会观察哪些容量和延迟指标？ ② 引入 Redis 后会新增哪些一致性与恢复问题？

**参考回答：** 当前点赞直接在 MySQL 事务中维护关系、计数和通知，规模不大时实现简单、事实一致。我会观察点赞接口 P95、热点行锁等待、数据库写 QPS、失败率和通知积压；只有这些指标证明 MySQL 热点成为问题，才考虑 Redis Lua。引入后虽然能降低请求延迟，但会增加 Redis 持久化、MQ 事件丢失、MySQL 投影延迟、全量校正和双写恢复问题。仓库里的 Redis 方案目前是优化设计，不是现有成果。

**来源：** S4、S5

### Q088｜P2｜八股延伸｜Java 服务端 / Redis

**主问题：** 如果使用 Redis Lua 同时维护点赞关系和计数，它能保证哪一范围内的原子性？

**连续追问：** ① Lua 成功但 MQ 事件未发出时怎么办？ ② Redis 与 MySQL 最终不一致时以谁为准？

**参考回答：** Lua 只能保证同一个 Redis 实例内脚本涉及的键原子更新，不能把 Redis、RabbitMQ 和 MySQL 变成一个事务。Lua 成功后如果发布 MQ 失败，需要可靠事件记录或补偿扫描，不能只依赖一次 publish。目标方案中 Redis 承担实时关系和计数，MySQL 保存持久投影，并通过定时全量校正发现差异；具体谁是权威必须在设计中明确。当前项目没有落地这条链路，所以我会把它作为方案分析，不会说已经解决。

**来源：** S5

### Q089｜P2｜面经原型｜前端 / Java 服务端

**主问题：** 图片流分页期间有新作品发布或旧作品删除，offset 分页和 cursor 分页分别会出现什么现象？

**连续追问：** ① 为什么游标通常需要包含排序字段和唯一 ID？ ② 搜索结果只允许较浅 offset 时，产品交互怎样配合？

**参考回答：** offset 在数据新增或删除后容易出现重复和跳项，越往后数据库或搜索引擎跳过的成本也越高。cursor 用上一次的排序字段加唯一 ID 形成稳定边界，例如 `publicAt + assetId`，同一时间的记录也能确定顺序，更适合瀑布流。关键词搜索当前使用 Meilisearch 的 offset，并限制较浅页数，因为搜索相关性结果本身可能变化；产品上采用“加载更多”并设置结果深度上限，而不是承诺无限翻页。

**来源：** S4、S6

### Q090｜P2｜八股延伸｜Java 服务端 / 安全

**主问题：** 用户输入一个远程图片 URL 让系统做图生图，会有哪些 SSRF、内容类型和资源大小风险？

**连续追问：** ① 为什么当前使用已授权资产 ID 能缩小攻击面？ ② 重定向后的目标地址是否需要再次校验？

**参考回答：** 任意远程 URL 可能指向内网、云元数据地址或超大文件，还可能伪造 Content-Type、通过重定向绕过首轮校验，形成 SSRF 和资源耗尽。AiVista 的图生图只接受当前用户已授权的资产 ID，由 Java校验归属，Worker 从受控的私有 OSS 读取，所以不需要替用户访问任意地址。如果未来开放 URL，必须限制协议、解析并校验每次重定向后的 IP、阻断内网网段、限制大小和超时，并以实际内容检测格式。

**来源：** S5、S6

---

## 8. 架构与优化设计（10 题）

### Q091｜P0｜简历直接追问｜架构 / Java 服务端 / Agent Worker

**主问题：** 为什么业务核心使用 Java，而 Agent Runtime 使用 TypeScript？

**连续追问：** ① 两个服务的所有权边界如何划分？ ② 跨语言后增加的协议、部署和排障成本是否值得？

**参考回答：** Java Core 已经承载认证、用户、社区、资产、额度和事务，适合继续作为业务事实拥有者；Pi Agent SDK 和模型接入在 TypeScript 侧更直接，所以独立成 AI Runtime。浏览器只访问 Java，Worker 只执行模型、Tool 和外部 I/O，并通过版本化命令与 Completion 协议交互。拆分确实增加了协议、两个运行时和联调成本，但也把长耗时、不稳定的 AI 执行与核心业务隔离。边界是否值得，取决于是否坚持“Java 管业务事实，TS 管执行”，而不是按语言随意拆服务。

**来源：** S2、S3

### Q092｜P0｜简历直接追问｜架构

**主问题：** 如果把系统全部合并成 Java 单体，或者全部改成 Node.js，会得到什么、失去什么？

**连续追问：** ① 小规模阶段拆服务是否过度设计？ ② 哪些模块即使合并部署也应该保持逻辑边界？

**参考回答：** 全部做成 Java 单体，部署更简单、事务边界更直观，但 Pi SDK 和现有 TypeScript Agent 生态需要重写；全部改成 Node.js 可以统一语言，却要迁移已经完成的 Spring Security、事务和社区领域。对个人项目来说拆两个后端有额外成本，所以我没有继续微服务化，只保留 Core 与 AI Runtime 这条必要边界。即使以后合并部署，业务状态与执行状态、创建命令与完成提交、实时事件与权威快照仍应逻辑分离。

**来源：** S2、S4

### Q093｜P0｜面经原型｜架构 / Java 服务端

**主问题：** 系统中谁是任务状态的唯一权威来源，为什么不能让 Worker 直接修改所有业务表？

**连续追问：** ① Worker Ledger 和 Java 业务状态冲突时如何判断？ ② 权威写入集中在 Java 对事务和权限有什么好处？

**参考回答：** Java 是任务、额度、资产和用户可见状态的唯一权威来源，Worker Ledger 只说明外部执行进度。Ledger 显示 STAGED 不代表用户已经拥有资产，必须等 Java Completion 事务确认；如果 Java 已取消任务，即使 Worker 有成功结果也不能覆盖。集中写入让权限、状态机、额度和资产在同一个事务内校验，也避免 Worker 获得修改社区业务表的权限。冲突时以 Java 快照决定是否继续提交、复用还是结束。

**来源：** S2

### Q094｜P0｜简历直接追问｜架构 / 优化

**主问题：** 当前架构扩展到多实例时，消息消费、实时连接、取消路由和进程内等待器分别要怎么演进？

**连续追问：** ① 哪些组件天然支持横向扩展，哪些依赖单实例假设？ ② 你会按什么顺序拆除这些限制？

**参考回答：** RabbitMQ 消费和无状态 REST 接口比较容易横向扩展，难点是 Java 内存里的 SSE 连接表、Worker 实例内的 Pi Session、取消路由和 Generation Completion 等待器。我的顺序会先补齐指标和任务到实例的登记，再把实时事件通过共享通道分发，把取消做成定向消息加共享状态，最后替换进程内等待器或用可恢复查询。幂等 Completion 和 Ledger 已经提供基础，但当前代码仍以 Java、Worker 各单实例为明确边界，我不会说它已经支持水平扩展。

**来源：** S2、S7

### Q095｜P1｜面经原型｜架构 / 可观测性

**主问题：** 如果只能选择三个核心指标监控 Agent 创作链路，你会选择什么？

**连续追问：** ① 怎样区分用户感知延迟和后台最终完成延迟？ ② MQ 积压、模型耗时和失败收敛如何关联到同一个任务？

**参考回答：** 如果只能选三个，我会看创建到首个可见事件的 P95、创建到业务终态的 P95，以及失败后在限定时间内收敛到明确终态的比例。第一个代表用户是否觉得系统有响应，第二个覆盖排队、模型和存储，第三个反映可靠性。排查时再下钻 Outbox 年龄、RabbitMQ ready/unacked、各阶段耗时和 Provider 错误。日志和指标都带 Creation ID、Generation Task ID、Completion ID 与 `toolCallId`，才能把跨服务链路拼起来。

**来源：** S2、C1

### Q096｜P1｜简历直接追问｜架构 / 容错

**主问题：** 当模型供应商、RabbitMQ、Meilisearch或 OSS 分别不可用时，系统应该怎样降级？

**连续追问：** ① 哪些故障只影响新任务，哪些会影响已有资产浏览？ ② 怎样避免重试风暴进一步放大故障？

**参考回答：** Provider 不可用时停止或限流新生成，已有资产仍可浏览；RabbitMQ 不可用时 Java 仍可事务创建任务和 Outbox，但任务保持排队，Dispatcher 退避重试；Meilisearch 不可用时搜索降级或返回明确错误，不影响资产详情；OSS 不可用既影响新结果转存，也可能影响图片访问。重试要按错误类别、指数退避和总预算控制，并结合熔断与队列背压。不能让所有 Worker 对同一个外部故障同时高速重试。

**来源：** S2、S4

### Q097｜P1｜面经原型｜前端 / Java 服务端 / Agent Worker

**主问题：** 你在这个项目中怎样使用 AI 辅助编码，又怎样验证 AI 生成代码没有破坏业务边界？

**连续追问：** ① 哪些代码必须由你亲自设计和审查？ ② 单元测试、集成测试和真实 Provider 冒烟分别能发现什么问题？

**参考回答：** 我把 AI 当成编码辅助，用于生成局部样板、测试草稿和帮助检索 API，但服务所有权、状态机、事务边界、失败矩阵、协议与权限必须由我设计并审查。单元测试验证纯逻辑和状态转换，集成测试验证数据库约束、事务和服务交互，Faux Provider 验证可控的 Agent 事件顺序，真实 Provider 冒烟发现模型兼容、图片输入和 Function Calling 差异。最终还要跑完整 E2E，并能解释任何一段关键代码为何这样写。

**来源：** S3

### Q098｜P2｜八股延伸｜架构 / Java 服务端

**主问题：** 如果 Java 服务出现 CPU 飙高、线程池耗尽或内存持续增长，你会按什么顺序排查？

**连续追问：** ① 长连接、阻塞远程调用和无界队列各有什么典型表现？ ② 线程 dump、GC 日志和链路追踪分别能提供什么证据？

**参考回答：** 我会先确认是单实例还是全局问题，再看 CPU、堆、GC、线程池、数据库连接池和请求延迟。CPU 高时用采样或线程 dump 找热点和忙循环；线程池耗尽时看线程是否阻塞在远程 I/O、锁或数据库；内存增长则结合 heap dump 和 GC 日志判断对象留存。大量 SSE 连接主要占文件描述符和连接对象，阻塞调用会让工作线程堆积，无界队列会表现为延迟和内存一起增长。链路追踪用于定位跨服务慢在哪一段，不能只靠重启掩盖问题。

**来源：** S2、S5

### Q099｜P2｜简历直接追问｜架构 / 优化

**主问题：** 如果只能保留一项架构复杂度，Outbox、实时事件链路和分级图片资产中你会保留哪一项？

**连续追问：** ① 判断依据是故障风险、用户体验还是资源成本？ ② 被删除的两项分别用什么更简单的方案替代？

**参考回答：** 如果只能保留一项，我会保留 Outbox 和幂等收敛，因为它直接决定用户任务会不会永久丢失或重复产生副作用。实时事件可以先退化成轮询任务快照，体验差一些但正确性还在；分级图片可以暂时只保留原图和一个展示图，牺牲部分流量和列表性能。这个选择基于故障不可逆性：体验优化可以逐步补，任务丢失、重复计费和状态错乱会直接破坏业务可信度。

**来源：** C3

### Q100｜P2｜面经原型｜架构 / 优化

**主问题：** 如果让你从零重做 AiVista，你会保留哪些设计，又会删掉或推迟哪些设计？

**连续追问：** ① 哪些复杂度已经被真实故障或测试证明必要？ ② 哪些优化必须等到有生产指标后再做？

**参考回答：** 我会保留 Java 作为权威核心、异步任务、Outbox、Worker Ledger、幂等 Completion、受限 Tool 和 REST 快照，这些都对应明确的崩溃、重复投递、权限或断线问题。初版会更早定义状态机和失败矩阵，少走“先调通再补可靠性”的弯路。多实例协调、Redis 点赞、完整 CDN 和复杂长期 Memory 会推迟到指标证明需要时再做。当前项目最大的经验是：可靠边界要提前设计，容量优化不要靠想象。

**来源：** S2、S4、C3

---

## 9. 数量与覆盖索引

| 模块 | 题号 | 数量 |
|---|---|---:|
| 项目真实性与开场 | Q001～Q008 | 8 |
| Agent 创作 | Q009～Q032 | 24 |
| 可靠执行 | Q033～Q056 | 24 |
| 实时交互 | Q057～Q074 | 18 |
| 资产闭环 | Q075～Q090 | 16 |
| 架构与优化设计 | Q091～Q100 | 10 |
| **合计** | Q001～Q100 | **100** |

来源类型目标配比：`面经原型 45`、`简历直接追问 35`、`八股延伸 20`。  
优先级目标配比：`P0 40`、`P1 40`、`P2 20`。

## 10. 使用边界

- 参考回答用于建立答题结构，不建议逐字背诵；面试时应结合实际演示、测试记录和个人经历调整措辞。
- CDN 私有源站、Redis 高并发点赞、跨实例协调、完整生产部署、监控告警、备份恢复与安全审计属于后续设计或生产前工作，不能表述为已经上线。
- RAG、微调、多 Agent、MCP 不属于 AiVista 当前核心实现，只用于选型对比或扩展追问。
- “真实面经”标签只说明问题角度来自第一人称面试记录；不得把改写后的 AiVista 题目宣称为对应公司的逐字真题。
- 回答题库时应始终以仓库当前代码和文档为事实来源，无法用实现、测试或数据证明的内容需要明确说明边界。
